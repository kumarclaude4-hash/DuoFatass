# Bug Tracker

Single source of truth for every security finding in this repo. It replaces the
two previously-separate tracking systems (`audit/` session reports +
`security-remediation/FINDING_INDEX.md` / `MASTER_CHECKLIST.md` /
`RISK_REGISTER.md` / `REMEDIATION_PROGRESS.md`), which had drifted out of sync
with each other and — in several rows — with the actual code.

**Baseline:** re-derived from source at commit `298f916` (2026-08-11), not
copied from the old trackers' claimed statuses. Every "Fixed" or "Open" below
was checked against the file(s) it names in this pass. The narrative session
reports under `audit/SESSION-*.md` and `security-remediation/sessions/` are
kept as historical investigation notes — this file is the only place that
carries a current disposition.

## Legend

- **Status** — `Open` (defect still present as read), `Partial` (some but not
  all of the defect is closed), `Fixed` (defect closed, checked against
  current source), `Accepted` (known limitation, deliberately not fixed —
  see note).
- **Sev** — governing severity (audit re-ratings applied, matching the old
  index): Critical / High / Medium / Low / Info.
- Findings ID'd `S0x-*` come from `audit/SESSION-0x-*.md`; `SC-*` from
  `SESSION-09-SUPPLY-CHAIN-CI.md`; `S10-*` from `SESSION-10-SYNTHESIS.md`.

## Corrections vs. the old trackers (read this first)

The previous `FINDING_INDEX.md` marked every one of these `open` as of its
last edit. Source inspection in this pass shows otherwise:

| ID | Old tracker said | Actual repo state |
|---|---|---|
| S08-C1 | open | **Fixed.** `release.yml` never writes `service-account.json`; landed in `5c2cd73`, which predates the audit itself — the tracker was simply never updated. |
| SC-02 | open | **Fixed.** Same commit — `local.properties` carries no secret values, and a CI step (`Assert no secrets in packaged build inputs`) fails the build if one appears. |
| S08-H1 | open | **Fixed.** `app/build.gradle` no longer emits `BuildConfig.WORKER_SECRET`; `worker/src/index.js` `/stats` now requires a separate `STATS_SECRET`, fail-closed if unset (`6c30267`, `fddd45c`). |
| S06-M1 | open | **Fixed.** `firestore.rules` `accountLock` `create` now requires `duressEligibility/{uid}.eligible == true` before a lock doc can be written — the enforcement gap the finding describes is closed. Re-verified (S3-10): `server/index.js`'s `/requestLockNonce` handler (`:3509-3515`) independently gates the same boundary server-side (404, not 403, on an ineligible uid, so refusal is indistinguishable from "endpoint does not exist" — the enumeration-resistance the finding asked for), checked *before* a nonce is generated. Previously had zero regression coverage; `server/lib/duressEligibilityWiring.test.js` now guards both the `/requestLockNonce` gate and the `firestore.rules` predicate (5/5 pass), falsified by temporarily reverting the rule (confirmed the test fails without the fix, then restored — no functional change made). |
| S05-M3 | open | **Partial.** Admin sessions now carry a 30-minute absolute TTL with expiry sweep (`ADMIN_SESSION_TTL_MS`, `server/index.js:662-675`). Still no bulk-revoke path and no binding to IP/UA. |

No other row changed disposition in this pass; all other "Fixed" rows below
match what the old index already claimed, re-confirmed against source.

## Verification confidence

- **Verified** — read against current source directly in this pass.
- **Carried** — the disposition matches the prior reconciled audit
  (`security-remediation/RECONCILIATION.md`), which itself was source-checked;
  not independently re-read line-by-line in this pass. Flagged so a future
  pass knows where to look first.

---

## Critical (4)

| ID | Status | Confidence | Issue | Evidence |
|---|---|---|---|---|
| S07-C1 | Fixed | Verified | `/mintToken` accepted a public value as ownership proof | `server/lib/identityVerify.js` + `challengeStore.js` exist; `index.js:1867` verifies signature, `:1853` consumes nonce; Android sends full `IdentityKeyPair` (`DisplayNameActivity.java:126`, `RestoreFromSeedActivity.java:226`). Android compile unverifiable (no JDK/SDK in this env) — treat as source-verified only. |
| S08-C1 | **Fixed** (S3-01) | Verified | Admin Firebase service-account key packaged into every APK | Re-verified from source this session (S3-01): no `service-account.json` write step in `release.yml`, `build-release.sh`, or `build-apks.sh`; all three fail the build if the file reappears. `find . -name service-account.json` → none; `assets/` holds only `README.txt`/`brand`/`watch_together`. Code half done; operator must still **revoke the leaked GCP SA key** (runbook). |
| SC-01 | **Fixed** (S3-02) | Verified | Vendored `libsignal-client-*-stripped.jar` not reproducible/hashed/validated in CI | Claim re-derived from source this session, then fixed. **Root cause confirmed live:** the committed `scripts/strip_signal_records.py` declared a 6-entry `STRIP` set, but an entry-by-entry diff of the shipped JAR against upstream `org.signal:libsignal-client:0.54.1` (fetched from Maven Central, sha256 `9605b9c6…87d4a`, matching the audit) showed **10 removed, 0 added, 0 CRC-changed** — `ChatService`, `Svr3`, `GroupSendEndorsementsResponse` and `ChatService$InternalRequest` had no documented provenance. **Fix:** `STRIP` corrected to the 10 real entries; the source JAR is now fetched from Maven Central and hash-pinned (`UPSTREAM_SHA256`) instead of scraped from `~/.gradle/caches`; the output is asserted against `EXPECTED_OUTPUT_SHA256` = `fa7d3afe…d89e3`; new `--check` mode. Hash recorded in-repo at `app/libs/libsignal-client-0.54.1-stripped.jar.sha256`. Gate wired as a dedicated `verify-libsignal-jar` job in `ci.yml` that `lint` needs (so `build-debug`/`instrumented-tests` inherit it) **and** in `release.yml` before the keystore is decoded. **Evidence (commands run this session):** full regenerate printed `Output : sha256 fa7d3afe…d89e3 (38466351 B) — matches recorded hash` and `git status` then showed the JAR **unmodified** — i.e. the documented procedure now reproduces the shipped binary byte-for-byte; `--check` exits 0 on the committed JAR, exits 1 with a `::error::` on a 1-byte append (`908298e2…62f5`), and exits 1 when the `.sha256` sidecar is absent; `js-yaml` parse of both workflows OK. Note: strip-by-prefix (the audit's alternative) would remove **37** entries and would *not* reproduce the artifact, so the explicit 10-entry list is intentional — do not "simplify" it. Commit `dcf85c5`. Residual (audit rec. 4, not this finding): deleting the vendored JAR entirely by raising `minSdk` to 34 remains the preferred long-term fix. |
| SC-02 | **Fixed** (S3-01) | Verified | Release workflow baked full backend secret set into shipped APK | `release.yml` and `ci.yml` write `local.properties` with non-secret routing values only (`b2.bucket`/`b2.region`/`push.server.url`/`worker.url`); `app/build.gradle` no longer reads the B2 credential pair (`B2_KEY_ID`/`B2_APPLICATION_KEY` emitted as `""`). `release.yml` "Assert no secrets in packaged build inputs" guard blocks `b2.key.id`/`b2.application.key`/`worker.secret`/private-key blocks. This session (S3-01) added the **same guard to `ci.yml`'s `build-debug` job**, which also uploads an APK artifact — closing the enforcement gap on the PR-facing job. Guard logic functionally tested: passes clean/empty-value, fails on real `worker.secret` value and on a private-key block. |

## High (30)

| ID | Status | Confidence | Issue | Evidence |
|---|---|---|---|---|
| S08-H1 | **Fixed** (S3-01) | Verified | `WORKER_SECRET` in `BuildConfig`, accepted on Worker `/stats` | Re-verified this session (S3-01): no `buildConfigField ... WORKER_SECRET` in `app/build.gradle`; grep of `.github/workflows/`, `build-*.sh`, `app/build.gradle` finds **no live WORKER_SECRET value injection** (all references are historical comments). `worker/src/index.js` `/stats` gate reads `env.STATS_SECRET` only, fail-closed if unset (`isStatsAuthorized`, `:88-99`), and `grep "env.WORKER_SECRET" worker/src/index.js` → **none**: the Worker has no WORKER_SECRET acceptance path. Operator must still rotate the leaked `WORKER_SECRET` (runbook), but it authorizes nothing now. |
| S07-H1 | **Fixed** (S3-06, pre-existing) | Verified | Mint key-check fails open when stored hash absent | Re-verified from source this session (source beats tracker — was stale-`open`). `server/index.js:2110-2121`: the existing-account branch now requires `typeof storedHash === "string" && storedHash.length === 64` before comparing; a missing/empty/non-string `identityPubKeyHash` throws the same "Key mismatch" 403 rather than passing (comment explicitly labeled `S07-H1 fail-closed` in source). Fixed in commit `5c2cd73` (predates this Round-3 tracker; not reimplemented, not re-tested — no new code, tracker corrected in place per protocol). |
| S06-H1 | Fixed | Verified | `accountLock` not enforced server-side | `server/index.js:2053-2062` checks `accountLock` inside the mint transaction before issuing a token. |
| S03-H1 | Fixed | Verified | Media scope confusion via client-created `groups/{chatId}` | `server/lib/mediaScope.js` exists; `firestore.rules` `groups` create now requires `!exists(chats/{groupId})` + `createdBy` checks. |
| S04-H1 | Fixed | Verified | SSRF predicate never resolved DNS / missed IPv6 | `server/lib/egressGuard.js` + test file exist; wired into `fetchFollowingSafeRedirects()`. Residual DNS-rebinding gap is documented in-code, not hidden. |
| S04-H2 | Fixed | Verified | `/linkPreview` unbounded body read, no timeout | `readCappedBody()` in `egressGuard.js`, wired at both HTML and image call sites. |
| S04-H3 | Fixed | Verified | `og:image` fetched directly by both devices — recipient IP/read-time beacon | `server/lib/imageProxy.js` signs a same-origin proxy URL instead of returning the sender's raw URL. |
| S08-H2 | **Partial** (S3-18) | Verified | `FLAG_SECURE` cleared app-wide | Claim re-confirmed before fixing: `BaseActivity.java:45`, `LockScreenActivity.java:68`, `MainActivity.java:41`, and `SecurityPrivacySettingsActivity.java:361` all unconditionally called `clearFlags(FLAG_SECURE)` — screenshots, screen recording, and the recents-list thumbnail were always allowed app-wide regardless of the `app_screenshot_enabled` preference (whose own default is `false`, i.e. secure-by-default, in `SecurityPrivacySettingsActivity`'s `getBoolean` call). Two other files (`GroupChatActivity.java`, `ChatMediaActivity.java`) already carried comments claiming "FLAG_SECURE is applied globally in BaseActivity.onCreate() based on the app_screenshot_enabled preference" — i.e. this was the intended design, just not actually wired. **Fix:** new package-private `BaseActivity.applyScreenshotSecurity(Activity)` reads `app_screenshot_enabled` from `duoshield_prefs` (default `false`) and sets or clears `FLAG_SECURE` accordingly; `BaseActivity.onCreate()`, `MainActivity.onCreate()`, and `LockScreenActivity.onCreate()` (the latter two extend `AppCompatActivity` directly, not `BaseActivity`, so each needed its own call site) all route through it, making the two pre-existing comments in `GroupChatActivity`/`ChatMediaActivity` actually true. `SecurityPrivacySettingsActivity.applyScreenshotFlag(boolean allow)` — a separate, activity-local immediate-effect helper called from the switch's `onCheckedChangeListener` — previously ignored its own `allow` parameter and always cleared the flag; now actually branches on `allow` so toggling the switch off takes effect on that screen immediately, without waiting for a recreate. Unused `WindowManager` imports removed from `MainActivity.java`/`LockScreenActivity.java` after removing their direct `clearFlags` calls. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK in this environment — source-reviewed and structurally checked (brace/paren/bracket balance), not compiled or test-executed; AND-lane verification routed to **S3-19b**, same as every other AND-lane finding in this program. See `SESSION-S3-18.md`. **REGRESSED in `9196a75` (PR #117), deliberately left in place — DO NOT RELEASE AS-IS:** both `getBoolean("app_screenshot_enabled", …)` call sites — `BaseActivity.applyScreenshotSecurity()` and `SecurityPrivacySettingsActivity` — had their default flipped `false` → `true` "for the testing phase", so `FLAG_SECURE` is now *cleared* unless a user opts back in. That reverses the secure-by-default posture this row's fix established: on a fresh install of a secure-messaging app, screenshots, screen recording, and the recents-list thumbnail are all permitted again with no user action. Decision taken this pass (explicitly, not by omission): **keep the allow-by-default while UI work on the chat surfaces is in flight, and track the revert here rather than gating it behind `BuildConfig.DEBUG`** — a debug gate was considered and rejected for now because it would make the on-device behaviour differ between the debug APK the UI work is checked on and the release APK, which is the opposite of what the testing phase needs. **Release blocker:** before any production/real build, flip both defaults back `true` → `false` and re-verify that a fresh install starts with `FLAG_SECURE` set. This row stays `Partial` until that flip lands. — **UPDATE (readiness-fix pass): the release-blocking flip has landed.** Verified against current source: `BaseActivity.applyScreenshotSecurity()` reads `getBoolean(KEY_APP_SCREENSHOT_ENABLED, false)` and takes the `addFlags(FLAG_SECURE)` branch when screenshots are not allowed, and `SecurityPrivacySettingsActivity` reads `getBoolean("app_screenshot_enabled", false)` for the switch's initial state — the two defaults are back in lockstep, so the switch cannot render out of step with the flag actually enforced on a fresh install. `applyScreenshotFlag(boolean allow)` still honours its parameter, and both persist-then-apply call sites (the no-PIN branch and the `promptCurrentPin` success callback) write the pref and pass the same value. A `Do not flip this to true again` note now sits in the `BaseActivity` javadoc, directing any future testing phase to the runtime Settings toggle instead of the default. **Still `Partial`, not `Fixed`,** for exactly the reason the original entry gives: this environment has no JDK/Android SDK (`java` absent, `ANDROID_HOME` unset, no root to install one), so this is source-verified only — the fresh-install on-device check that `FLAG_SECURE` is actually set has **not** been run. Promote to `Fixed` only after that check passes on a real device. |
| S08-H3 | **Partial** (S3-18) | Verified | Plaintext media persists in 150 MB Glide disk cache | Claim re-read and re-scoped this pass (previous pass carried it without re-reading): message images/videos are decoded through Glide via `Glide.with(ctx).load(byte[])` in `MessageAdapter.java` (decrypted bytes from `B2StorageHelper`), and `DuoShieldGlideModule` persists the decoded result to an on-disk `DiskLruCache` at `getCacheDir()/glide_image_cache`, capped at 150 MB (75 MB low-RAM) — independent of the Room row, the Firestore doc, and the encrypted B2 blob. `WipeHelper.eraseLocalData()`'s step 9 (`deleteDir(appCtx.getCacheDir())`) already reaches this directory on every full wipe (voluntary/unpair/duress), so the wipe path was already closing this gap before this session. The concrete, still-open gap this session found and fixed is narrower and separate from wipe: `SelfDestructWorker` (disappearing messages) deleted the expired message's Room row, Firestore doc, and encrypted B2 blob, but never touched Glide's disk cache — so a message that had "disappeared" everywhere else still left a readable, unencrypted decoded copy of its image/video frame sitting on disk, for as long as 150 MB of LRU pressure allowed. **Fix:** `MessageDao.deleteExpired()` changed from `void` to `int` (Room supports returning the affected-row count for an `@Query` DELETE); `SelfDestructWorker.commitBatchDelete`/`deleteExpiredFromFirestore` likewise now return a count instead of `void`; `SelfDestructWorker.doWork()` calls `Glide.get(context).clearDiskCache()` only when a pass actually deleted at least one message (Room or Firestore), so an ordinary 15-minute poll with nothing expired does not evict unrelated, still-live thumbnails. `clearDiskCache()` runs inside `Worker.doWork()`, which WorkManager already executes off the main thread, satisfying Glide's own threading requirement for that call. This clears the *entire* Glide disk cache rather than a per-message-key entry — deliberately, mirroring `WipeHelper`'s own same tradeoff (no per-key Glide cache eviction exists anywhere in this codebase; constructing one is out of scope for this session and unverifiable without a compiler). **Temp-prefix sweep (follow-up, commit `5171495`):** the second half of this finding — `TempFileCleaner.isTempMediaFile()` only swept `voice_`/`vid_`/`share_`/`duoshield_export_` prefixes from `getCacheDir()`, missing other plaintext scratch — is now addressed. Source-verified `File.createTempFile`/`new File(getCacheDir(),…)` call sites: added `cam_*.jpg` (`ChatMediaActivity.java:2459`) and `grp_cam_*.jpg` (`GroupChatActivity.java:859`) — plaintext camera captures handed to the camera app via `FileProvider` that DuoShield **never deletes itself**, the genuine leak; added `thumb_*.mp4` (`B2StorageHelper.java:783`, plaintext frame-extraction scratch) and `enc_*.tmp` (`ChatMediaActivity.java:2748`/`GroupChatActivity.java:932`, ciphertext upload scratch) as crash-orphan defense since both are normally deleted in a `finally`. `vid_view_*.mp4` (`MediaViewerActivity.java:81`) needed no rule — it already matches the existing `vid_` prefix. `b2dl_*.enc` (`B2StorageHelper.java:1964`) deliberately **not** added: it is created in `dest.getParentFile()` inside the `chat_export_<ts>/…` working dir (a cache subdirectory) and deleted in a `finally`, and any crash-orphan is already removed by the existing recursive `chat_export_` directory sweep — a top-level rule would be dead code. **Residual/out of scope:** a full audit of every other place plaintext could linger outside Glide's disk cache (e.g. Android's own thumbnail/media-store caches, `adb backup` reachability of `getCacheDir()` if `allowBackup` were true — verified elsewhere as already `false`) was not performed this session. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — source-reviewed and structurally checked (brace/paren/bracket balance PASS on `TempFileCleaner.java`), not compiled/tested; AND-lane verification routed to **S3-19b**. See `SESSION-S3-18.md`. |
| S08-H4 | Fixed | Verified | Link-preview image fetched from sender's host (client half of S04-H3) | Closed server-side without a client change — `MessageAdapter.java`/`LinkPreviewFetcher.java` take the URL verbatim from `/linkPreview`, which now returns the server's own signed proxy URL. |
| S08-H5 | Open | Verified (S3-16) | `SecurePrefs` plaintext fallback holds identity key, backup key, SQLCipher passphrase | Re-confirmed from source this session, not code-changed. `SecurePrefs.buildTiered()` still ends in a plaintext `MODE_PRIVATE` fallback after all 3 ESP tiers fail (class javadoc + fallback branch). The plan's literal sub-claim — `isInitialized()` must not ignore the fallback — is confirmed ALREADY correct and deliberate: `SignalKeyManager.isInitialized(ctx)` reads via `SecurePrefs.get(ctx)` regardless of tier and is explicitly NOT gated on `isAvailable()` (inline comment cites BUG-S10: gating on `isAvailable()` caused a permanent sign-in loop on fallback devices where keys legitimately live in plaintext) — not a regression to fix. Root defect remains genuinely open: `BackupCryptoHelper.storeKey()` writes the backup key into `SecurePrefs` unconditionally (no `isAvailable()` guard) while `getStoredKey()` refuses to read it back once `!SecurePrefs.isAvailable()` — a plaintext copy of the backup key sits on disk with no functional benefit either way. Removing the fallback outright is a documented product tradeoff (avoids a hard lockout on budget/Android-Go devices) needing a user-facing warning/consent flow — out of scope for this AND-lane source-only session (S3-16). See `SESSION-S3-16.md`. |
| S06-H2 | Fixed | Verified (carried from audit, spot-checked) | Duress wipe left WorkManager residue correlating to duress | `AccountLockWorker`/`FcmUnregisterWorker` input data carries no duress-identifying field per audit re-check; not independently re-read this pass beyond confirming files exist. |
| S06-H3 | Fixed | Carried | Offline duress trigger never locks | `DuressManager.maintainLockCredential()` call site added per audit record; not re-read this pass. |
| S05-H1 | Fixed | Verified | `ADMIN_TOKEN` no entropy floor / no brute-force ceiling | `server/lib/adminSecret.js` + test exist; `evaluateSecretStrength()` enforces a 128-bit floor at startup. |
| S05-H2 | **Partial** (S3-13) | Verified (S3-13 continuation; re-verified S3-13 E2E-check session) | Waitlist unreviewable — no deny/expire/revoke path | New `POST /admin/api/waitlist/deny` (mirrors `/approve`'s shape: `requireAdminAuth`, requestId format check, 404/409 on not-found/not-pending) flips a pending doc to a new `status: "denied"` state, logs `reqTag(requestId)` (not the raw id), and audits `waitlist_denied` through `auditAdminEvent()`; admin panel gained a "Deny" button next to "Approve" and an audit-log label for the new action. `GET /waitlistStatus` needed no change — it already returns whatever `status` string the doc holds, so `"denied"` flows through untouched, and `/mintToken`'s `status === "approved"` check already rejects it. Structural wiring test in `server/lib/adminAuditWiring.test.js` (auth-gated, `denied` status, audit call, all in the right handler). A follow-up E2E-check session read the full chain from the UI down (not just the wiring test) and confirmed it is real, not mock: admin panel `denyBtn.onclick` → `deny()` → `api("/admin/api/waitlist/deny", ...)` (sends the `x-admin-token` header + same-origin session cookie, `index.js:1415-1445`) → the route handler (`index.js:4013-4053`) calls the real `requireAdminAuth(req, res)` gate (token-or-session check, IP lockout, `admin_api_unauthorized`/`admin_api_blocked_locked_out` audit on failure, `index.js:904-938`) → on success does a real Firestore `ref.update({ status: "denied", deniedAt: ... })` mutation on the `waitlist/{requestId}` doc → calls `auditAdminEvent("waitlist_denied", req, { requestId })`, which pseudonymises `adminIp`/logs `reqTag(requestId)` and durably writes to `adminAuditLog` (client-read/write denied by `firestore.rules`) → the UI's `toast()` + `loadWaitlist()`/`loadAuditLog()` refresh reflect the new `denied` status and the new audit row. No mock/stub short-circuits any link in that chain. `npm test` (server) re-run this session: **197/198 pass**, same pre-existing unrelated `identityVerify.test.js` native-module failure, not a regression; no code changes were needed. **Still open, not attempted this or the prior session** (out of S3-13's narrower "deny-path" exit bar): the finding's broader design fix (invert to operator-issued invites, or require reviewable request content), expiry on approved-but-unused invites, revoke-an-approval, pagination/total-count so a flood is visible rather than silently truncating the operator's view, and surfacing `usedByUserId` linkage in the panel. |
| S05-H3 | Fixed | Verified | Admin actions not durably audited; admin auth not audited at all | `server/lib/adminAuditWiring.test.js` exists; 7 audit call sites confirmed wired into `requireAdminAuth`/login/logout paths. |
| SC-04 | **Fixed** (S3-02) | Verified | Release APKs unverifiable — no checksums/provenance | Claim re-confirmed live before fixing: `grep -iE "sha256|checksum|attest|provenance"` across `.github/` returned **only two SC-05 comment lines**, no functional step — releases really did attach raw APKs alone. **Fix in `release.yml`:** new "Generate SHA256SUMS and record signing certificate" step writes `SHA256SUMS` with bare filenames (so `sha256sum -c SHA256SUMS` works in the user's download dir) and extracts the signing-certificate SHA-256 with `apksigner verify --print-certs`; `actions/attest-build-provenance@v2` attaches signed provenance tying each APK to the run/commit (with the minimum `id-token: write` + `attestations: write` permissions added); a "Compose release body" step publishes checksums, the cert digest, and three concrete verification commands (`sha256sum -c`, `gh attestation verify`, `apksigner verify --print-certs`) via `body_path`; `SHA256SUMS` is attached alongside the APKs. Runs before the keystore-erase step (apksigner needs only the signed APK). **Fails closed:** missing APK dir, zero APKs, a failing `sha256sum`, or an empty `SHA256SUMS` all `exit 1` rather than publishing a release that merely *looks* verifiable; a cert digest that could not be read is omitted with a `::warning::` instead of being fabricated. **Evidence (commands run this session):** `js-yaml` parse OK and the publish step's `with:` confirmed to carry `body_path: release-verify-body.md` + both file globs; the step's shell logic replayed in a scratch tree — checksum roundtrip `sha256sum -c` → both `OK`, then a tampered APK → `FAILED` (nonzero exit); both empty-dir and no-APK guards exercised and each exited 1. Actual CI execution is operator-side (requires a push/dispatch), per the CI lane definition. `generate_release_notes: true` is retained — `body_path` takes precedence over `body` and is prepended to the generated notes (confirmed against the action's documented behavior). Commit `dcf85c5`. |
| SC-05 | **Fixed** (S3-02) | Verified | Workflow deletes all releases and tags on every push to `main` | The paginated "Delete all previous releases and tags" step (which enumerated every release and issued `DELETE …/releases/{id}` + `DELETE …/git/refs/tags/{tag}` for each, destroying all provenance/rollback history on every push) is **removed**. Replaced with "Clear existing release for this tag", which looks up only the single resolved rolling tag (`gh api repos/…/releases/tags/${TAG}`) and deletes at most that one stale release record; `action-gh-release` then re-points the tag in place. All other releases/tags are preserved. Verified: `js-yaml` parse OK; grep confirms no bulk enumeration (`per_page=100`/`releases?`) or `git/refs/tags` DELETE remains (sole match is a descriptive comment); deletion scoped to `releases/tags/${TAG}`. |
| SC-03 | **Partial** (S3-03) | Verified | No Gradle dependency verification | Claim re-confirmed before fixing: no `gradle/verification-metadata.xml` existed. **Fix (scaffold):** added `gradle/verification-metadata.xml` with `<verify-metadata>true` + `<verify-signatures>false`, `<trusted-artifacts>` for the Gradle wrapper distribution zips, and an operator runbook (in-file) to populate component hashes with `gradlew --write-verification-metadata sha256 help` and then flip signature verification on. **Why Partial, not Fixed:** the file has an empty `<components/>` set, so it does not yet *enforce* per-artifact hashes — populating them requires running Gradle against the real Android/Google/Maven dependency graph, which needs the Android SDK + network resolution and is a **BLOCKED gate** in this environment (same toolchain blocker as S3-19b). Landing the scaffold + runbook is the reproducible, in-repo half; hash population is deferred to the operator. **Evidence:** `xml.dom.minidom.parse` → well-formed; scaffold committed. |
| SC-06 | **Fixed** (S3-03) | Verified | JitPack in repo list — builds from mutable Git refs | Claim re-confirmed: `build.gradle` had an unscoped `maven { url 'https://jitpack.io' }` in `allprojects.repositories`, so JitPack (which builds artifacts on demand from mutable Git refs) could serve *any* coordinate. **Fix in `build.gradle`:** wrapped the JitPack declaration in a `content { includeGroupByRegex 'com\\.github\\..*' }` filter so it is only ever consulted for `com.github.*` groups; `google()` and `mavenCentral()` (declared first, content-addressed) remain the only sources for all androidx/Firebase/Google/glide artifacts. **Evidence:** `includeGroupByRegex` present at the JitPack block; brace balance verified (equal `{`/`}`). Full resolution confirmation is operator-side (needs Gradle + network), per the CI lane. |
| S02-H1 | **Fixed** (S3-06) | Verified | `migrateUid` copies `users/{oldUid}` verbatim | Claim re-confirmed before fixing: `server/index.js` `.set(data)`'d the entire `users/{oldUid}` document onto `users/{userId}` via the Admin SDK (bypasses Firestore rules) — any field an attacker managed to stash on the old doc (legacy doc, rule bug, or a rule-permitted-but-untrusted field) would be replayed with rule-bypassing privilege. **Fix (`server/index.js` + new `server/lib/profileSanitize.js`, commit 269d165):** `sanitizeMigratedUserFields()` copies only the same allow-listed, type/size-bounded fields the `users/{uid}` write rule itself permits (`displayName`/`fcmToken`/`platform`/`photoUrl`), all others dropped. **Evidence:** `lib/profileSanitize.test.js` (5 cases: allow-listed passthrough, field-injection dropped, wrong-type dropped, oversized dropped, missing/non-object doc tolerated) — `npm test` 146/147 pass (the 1 failure is the pre-existing, unrelated `identityVerify.test.js` missing-native-module case, documented in `SESSION_PROTOCOL.md` §0); `node --check index.js` clean; `grep sanitizeMigratedUserFields index.js` confirms live wiring at the `/migrateUid` call site. |
| S03-H2 | **Fixed** (corrected) | Verified | Per-token, not per-user, Worker rate bucket | `worker/src/index.js` now tracks `perUserCounts` keyed by client ID with per-minute buckets, separate from the raw daily cap. |
| S03-H3 | **Fixed** (S3-11) | Verified | No per-user storage quota | **Fix in `worker/src/index.js`:** per-user byte counters (`user:bytes:<holder>`) tracked in KV alongside the existing global R2 counter, gated by a new configurable `MAX_USER_BYTES` (default set in `wrangler.jsonc`); the uploader's holder ID (from the media-token capability, not client-supplied) is stored as R2 object metadata so DELETE can find whose quota to credit back. Both the pre-upload check (declared `Content-Length`) and a **post-upload re-check** (actual R2 `head()` size) are enforced — closing the case where a missing/understated Content-Length (e.g. a chunked/streamed body with no length) would otherwise sail past the pre-check and get stored anyway; the object is deleted and the write rejected (507) before any quota is credited if the true size would exceed either the global R2 cap or the per-user cap. DELETE decrements by the object's actual stored size (read from R2 metadata, not re-derived). R2→B2 migration credits the R2 quota back for the migrated holder so it does not leak away permanently. Counters use atomic KV increment semantics scoped per key (no separate read-modify-write round trip an attacker's two concurrent uploads could race between); the worst case for a genuine race is bounded — bytes counted twice for the same request would still be caught by the DELETE-time correction when metadata is re-read. **Evidence:** `worker/src/index.test.js` — 9/9 tests pass (`npm test`), including a new regression test that reproduces the Content-Length-lie bypass (a streamed body with no declarable length must still be rejected by the post-upload re-check, must not leave the object in R2, and must not consume any quota); `node --check src/index.js` clean. See `security-remediation/sessions/SESSION-S3-11.md`. **S03-H3 follow-up (fixed in S3-12):** a gap flagged during S3-12's S03-M2 work — once same-holder overwrite was allowed, the pre- and post-upload quota checks credited the *entire new size* on every overwrite without ever subtracting the old object's size, so N overwrites of a 10 MB file inflated both the global R2 counter and the holder's per-user counter by 10×N MB while R2 itself only ever held 10 MB; the counter never converged back down, and a *shrinking* replacement never freed the headroom it should have. **Fix in `worker/src/index.js`:** both the pre-upload projection and the post-upload re-check now compute `existingSize` via the HEAD already performed for the S03-M2 ownership check and subtract it before adding the new size (`userBytes - oldSize + declaredBytes`, and the equivalent for the global R2 counter and the post-upload `actualBytes` re-check); the final credit uses the delta (`actualBytes - oldSize`), which is unchanged behavior for a brand-new key (`oldSize` = 0). **Documented, not fixed:** this remains the same best-effort, non-atomic counter model as the rest of this file (no Durable Objects — see S03-I2) — two concurrent overwrites of the same key can still race between the HEAD and either write landing, and a rejected overwrite cannot roll back to the pre-overwrite bytes since R2 has no object versioning configured here and the destructive `put()` has already replaced them by the time the post-upload checks run. **Evidence:** 3 new regression tests (same-size overwrite does not inflate the counter; growing overwrite is charged only the delta; shrinking overwrite frees the headroom it gives back) — `npm test` **27/27 pass**. See `security-remediation/sessions/SESSION-S3-12.md`. |
| S07-H2 | **Partial** (S3-17) | Verified | Backup docs ship unkeyed SHA-256 of plaintext | Claim re-confirmed before fixing: `BackupCryptoHelper.computeChecksum`/`verifyChecksum` were a plain, unkeyed SHA-256 of the plaintext stored as the `checksum` Firestore field — an offline guess-confirmation oracle (anyone holding the digest and a guessed plaintext can confirm the guess with no key). **Fix (`BackupCryptoHelper.java`):** new `computeHmac`/`verifyHmac` — HMAC-SHA256 keyed with a value HKDF-derived from the backup key using a distinct `info` string (`DUOSHIELD_BACKUP_HMAC_V1`) from the AES-GCM key, verified with a constant-time `MessageDigest.isEqual`. **Wired, not just added:** `BackupManager.java`'s 4 write call sites now write `hmac` instead of `checksum`, and the restore path in `restoreAllSync` prefers `verifyHmac` for any doc carrying an `hmac` field, falling back to legacy `verifyChecksum` only for docs written before this fix (no `hmac` field). Legacy `computeChecksum`/`verifyChecksum` kept, `@Deprecated`, read-only for old docs. New unit tests in `BackupRoundTripTest.java` (plain JVM, no Android/Firebase dependency) cover HMAC compute/verify round-trip. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK in this environment (`which java/javac/gradle` empty) — source-reviewed and structurally checked (brace/paren/bracket balance), not compiled or test-executed; AND-lane verification routed to **S3-19b** per this program's standing exit criterion. See `SESSION-S3-17.md`. |
| S07-H3 | **Partial** (S3-17) | Verified | Group messages have no AAD | Claim re-confirmed before fixing: `GroupCipherHelper.encrypt`/`decrypt` had no `updateAAD` call — a group's single shared AES key meant a ciphertext written for one (group, sender, message id) could be replayed/spliced into another slot the same key protects, with no crypto-layer binding (only Firestore rules constrained this). **Fix:** new `GroupCipherHelper.buildAad(groupId, senderUid, messageId)` + 3-arg `encrypt`/`decrypt` overloads that call `Cipher.updateAAD`; `GroupChatActivity`'s send path builds the AAD from `(groupId, myUid, msgId)` and calls the AAD-aware `encrypt`, tagging the doc `"aadV1": true`; the receive path reconstructs the identical AAD from the message's own `(groupId, sender, id)` fields **only** when `aadV1` is present, so a legacy message (no bound context) still decrypts with no AAD exactly as before. Legacy 2-arg `encrypt`/`decrypt` kept for those old messages. New `GroupCipherHelperAadTest.java` (plain JVM unit tests) covers matching-AAD round trip and tamper/mismatch rejection. **Two lanes:** (1) **AND** — still open; no JDK/Gradle/Android SDK here; source-reviewed and structurally checked, not compiled/tested; routed to **S3-19b**. (2) **RULES** — this fix is crypto-layer only; the plan's paired Firestore-rules half of this finding (binding sender/group at the rules layer) was never attempted in code this session, so there is no rules-side change to verify — S3-15b's emulator run (186/186 pass, this session) confirms the *existing* rules regression suite still holds, but the rules-layer binding this finding calls for remains **unimplemented**, tracked separately, not closed by S3-15b. Neither lane closing lets this be marked `Fixed`. See `SESSION-S3-17.md`. |
| S04-M1 (gov: High) | **Fixed** (S3-07 cont'd) | Verified | IPv6 /64 defeats IP-keyed limits | Deferred earlier in the S3-07 session (see prior tracker text, corrected here), then implemented before the session closed: new pure `normalizeIpForRateLimit(ip)` in `server/lib/pure.js` (commit `959d869`) collapses an IPv6 address to its /64 prefix (unwrapping IPv4-mapped and stripping zone indices first; malformed input returned unchanged) so a residential /64 rotating its last 64 bits can no longer get one rate-limit/lockout bucket per request. Wired into every IP-keyed limiter in `server/index.js`: `ipHits`, `waitlistIpHits`, `waitlistPollHits`, and the admin lockout (`adminIpFails` at the time, now `adminLockoutStore` — see S04-L3). **Evidence:** `lib/pure.test.js` +14 cases (loopback/IPv4/IPv4-mapped unchanged, `::` expansion at every position, zone-index/bracket stripping, two addresses in the same /64 collapse to one key, malformed input left as-is); `npm test` 194/194 pass (see S04-L3 evidence — count reflects both this and the S04-L3 session's additions); `node --check` clean; `grep normalizeIpForRateLimit index.js` confirms all four call sites. |
| S01-H1 | **Fixed** (S3-04, emulator-verified S3-15b) | Verified | Cross-user prekey wipe/replace | Claim re-confirmed before fixing: the cross-user `public_keys` update scoped only `affectedKeys().hasOnly(['oneTimePreKeys','updatedAt'])` — WHICH fields, not the VALUES. **Fix (`firestore.rules`, commit 812813d):** the cross-user branch now additionally requires `request.resource.data.oneTimePreKeys.size() < resource.data.oneTimePreKeys.size()`, i.e. the pool may only SHRINK. Legit consumption is `FieldValue.arrayRemove` (`SignalSessionManager.consumeOtpkWithRetry:361`), which only ever decreases size; owner writes (`auth.uid == uid`) stay unconstrained for refresh growth (`arrayUnion`). This blocks wipe-to-larger, same-size attacker-prekey swap, and pool growth by a stranger. Residual (accepted, documented in-rule): rules cannot pin individual array elements, so a stranger could still shrink to a *different* smaller set — `arrayRemove` never does this and the size-invariant closes the High vectors. **S3-15b (this session):** installed Java 21 + firebase-tools 15.26.0 and ran `firebase emulators:exec --only firestore` against `firestore-tests/` in full — all 4 tests for this finding (consume shrink → succeeds; grow/same-size-swap by non-owner → fail; owner grow → succeeds) passed against the live emulator, part of a 186/186 full-suite pass. Promoted from Partial to Fixed per the S3-15b gate. |
| S01-H2 | **Fixed** (S3-04, emulator-verified S3-15b) | Verified | 1:1 message content mutable on update | Claim re-confirmed: the `chats/{chatId}/messages` `update` rule pinned only `sender` (+ `deletedForAll` sender-gate); the ciphertext body had no protection, so a **recipient** could overwrite the `text` of a message they did not send. **Fix (`firestore.rules`, commit 812813d):** on update, `text`/`sigType`/`type` may change only when `resource.data.sender == request.auth.uid` (the legitimate sender-only 48h edit path — `EditMessageHelper.canEdit` gates on `myUid.equals(sender)`, re-encrypts, updates `text`/`sigType`), and `isEncrypted` is pinned immutable (`request.resource.data.isEncrypted == resource.data.isEncrypted`) so no participant can downgrade an encrypted message to plaintext. Non-content fields (reactions/status/expiresAt/deletedForAll) remain updatable by either participant under the existing guards. Group messages are already append-only (no `update`), so this is scoped to 1:1 per the finding. **S3-15b (this session):** ran the 4 tests for this finding (recipient rewrite → fail; recipient downgrade → fail; sender edit → succeeds; recipient status update → succeeds) against the live Firestore emulator — all passed, part of the 186/186 full-suite run. Promoted from Partial to Fixed per the S3-15b gate. |
| S01-H3 | **Fixed** (S3-04, emulator-verified S3-15b) | Verified | Partner display-name overwrite | Claim re-confirmed: the chat `update` `hasAny` block-list (`typing_`/`online_`/`lastSeen_`/`unread_`) had no `partnerName_`/`partnerPhotoUrl_` entry. **Fix (`firestore.rules`, commit 812813d):** added `'partnerName_' + request.auth.uid` and `'partnerPhotoUrl_' + request.auth.uid` to the block-list. Note the INVERTED convention (verified in source): a participant writes the key suffixed with the PARTNER's uid — their own name/photo, for the partner to read (`SettingsActivity.propagateName:326` → `update("partnerName_" + partnerUid, myName)`) — and only READS the key suffixed with their OWN uid (`ConversationListActivity:361`). So the field a participant must not touch is the self-suffixed one (partner-owned); the legit partner-suffixed write is unaffected. **S3-15b (this session):** ran the 3 tests for this finding (participant sets partner-suffixed name → succeeds; overwrites self-suffixed name → fail; overwrites self-suffixed photo → fail) against the live Firestore emulator — all passed, part of the 186/186 full-suite run. Promoted from Partial to Fixed per the S3-15b gate. |
| S07-M1 (gov: High, = S08-H5) | Open | Verified (S3-16) | SecurePrefs plaintext fallback | Same defect as S08-H5 above, same disposition this session — genuinely open, source-confirmed, out of scope to fully remediate under S3-16 (needs a product-level UX decision, not just a code change). |

## Medium (26) — was 25 (corrected S3-20; previously misstated as 26, see Summary reconciliation note); S06-M6 added below as a new row, not a restoration of the old miscount

| ID | Status | Confidence | Issue |
|---|---|---|---|
| S01-M1 | **Fixed** (S3-05, emulator-verified S3-15b) | Verified | Group message TOCTOU + no volume cap | Claim re-confirmed: `groups/{id}/messages` create had no cap on `text` size — any member could bulk-write oversized documents (cost/DoS). **Fix (`firestore.rules`, commit 3070b0b):** create now additionally requires `request.resource.data.text is string && text.size() < 100000`. The TOCTOU half (membership checked via `get()` at write time, independent of a concurrent member-removal write) is an inherent property of every `get()`-based membership check in this ruleset — not fixable from within a single rule evaluation — and remains **accepted** (documented in-rule per the audit's own recommendation, see S01-I2). **S3-15b (this session):** ran the volume-cap tests (oversized text → fail; boundary-size text → succeeds; non-string text → fail; empty-text media message still succeeds) against the live Firestore emulator — all passed, part of the 186/186 full-suite run. Promoted from Partial to Fixed (the volume-cap fix; TOCTOU stays accepted, not a fix target) per the S3-15b gate. |
| S01-M2 | **Fixed** (S3-05, emulator-verified S3-15b) | Verified | `identities` update has no field allow-list — rule only pins `uid`/`identityPubKeyHash`, no `hasOnly` on the full field set | Claim re-confirmed: `identities/{userId}` is world-readable (any signed-in user), so an unconstrained update let the owner publish arbitrary attacker-controlled fields into a globally-readable doc. **Fix (`firestore.rules`, commit 3070b0b):** update now additionally requires `request.resource.data.keys().hasOnly(['uid','identityPubKeyHash'])`. Grepped every client call site — no code ever writes any other field to this doc, so this closes the hole with zero functional impact. **S3-15b (this session):** ran the allow-list tests (extra field → fail; stored-XSS-shaped field → fail) plus the pre-existing hijack/pinning tests against the live Firestore emulator — all passed, part of the 186/186 full-suite run. Promoted from Partial to Fixed per the S3-15b gate. |
| S01-M3 | **Fixed** (S3-05, emulator-verified S3-15b) | Verified | `backup_logs` create unbounded/unvalidated — rule only checks `uid == auth.uid`, no shape/size validation | Claim re-confirmed: only ownership was checked — no field allow-list, no event enum, no size bound. An authenticated caller could write unbounded/arbitrary-content documents (write-amplification/storage-cost DoS, stored-content-injection for any downstream log consumer). **Fix (`firestore.rules`, commit 3070b0b):** create now validates the exact shape `BackupManager.logEvent` writes — `keys().hasOnly(['uid','event','ts','count','error'])`, `event` restricted to the four known literal values, `ts`/`count` must be numbers, optional `error` capped at 2000 chars. Rules cannot rate-limit document *count* — that residual relies on server-side/quota controls, documented in-rule (accepted). **S3-15b (this session):** ran the 6 shape-validation tests (real logEvent shape incl. optional error → succeeds; extra field → fail; unknown event → fail; missing required field → fail; oversized error → fail; non-numeric count → fail) against the live Firestore emulator — all passed, part of the 186/186 full-suite run. Promoted from Partial to Fixed per the S3-15b gate. |
| S01-M4 | **Fixed** (S3-05, emulator-verified S3-15b) | Verified | Group message delete has no membership re-check — rule checks `sender == auth.uid` only | Claim re-confirmed: delete checked only `resource.data.sender == auth.uid`, unlike read/create which both re-check current membership via `get()` on the group doc. A member removed from the group could still delete their own historical messages post-departure. **Fix (`firestore.rules`, commit 3070b0b):** delete now additionally requires `request.auth.uid in get(/databases/$(database)/documents/groups/$(groupId)).data.members`, mirroring the read/create guard. **S3-15b (this session):** ran the 3 tests for this finding (member-sender delete → succeeds; non-sender member delete → fail; removed-ex-member delete of own historical message → fail) against the live Firestore emulator — all passed, part of the 186/186 full-suite run. Promoted from Partial to Fixed per the S3-15b gate. |
| S02-M1 | **Fixed** (S3-06, pre-existing) | Verified | Mint cooldown stamped pre-auth | Re-verified from source this session, per SESSION_PROTOCOL §3 ("source beats tracker") — this was stale-`open` in the tracker but already remediated. `server/index.js:1984-1999` reserves the cooldown slot *before* the nonce-consume/signature-verify/transaction (closing the original race that let concurrent requests both pass), and releases it back to the prior timestamp (`releaseCooldown()`, not delete) on every rejection path (bad nonce, bad signature, locked account, key mismatch) so a failed/unauthenticated guess never burns the legitimate owner's cooldown into a DoS. Fixed in commit `5c2cd73` (predates this Round-3 tracker; not reimplemented, not re-tested — no new code, tracker corrected in place per protocol). |
| S03-M1 | **Fixed** (S3-12) | Verified | Attacker `Content-Type` stored/echoed, no `nosniff`/`Content-Disposition` — neither header found in `worker/src/index.js` | Claim re-confirmed before fixing: PUT stored `request.headers.get('Content-Type')` verbatim (fully attacker-controlled — nothing validated it against the key's extension) and GET replayed that stored value unmodified, with no `X-Content-Type-Options` and no `Content-Disposition` on either the R2 (hot) or B2 (cold) response path. A `write`-token holder could store an object at `…/x.jpg` served as `text/html`/`image/svg+xml` with attacker-chosen bytes. **Fix in `worker/src/index.js`:** new `contentTypeForKey(key)` derives the Content-Type purely from the already-allow-listed key extension (`jpg→image/jpeg`, `mp4→video/mp4`, `m4a→audio/mp4`, `3gp→video/3gpp`) via a fixed lookup table — the client's Content-Type header is no longer read at all, at PUT *or* GET time, on either tier. This also retroactively neutralizes any object stored with an attacker-chosen type before this fix shipped, since GET no longer reads the stored/upstream value. Both GET response paths (R2 and B2) also now set `X-Content-Type-Options: nosniff` and `Content-Disposition: attachment`. **Evidence:** `worker/src/index.test.js` — 2 new regression tests (hot-tier PUT declares `text/html`, GET still serves `image/jpeg` with nosniff+attachment; cold-tier B2 upstream declares a malicious type, GET still serves the key-derived type) — `npm test` **27/27 pass**; `node --check src/index.js` clean. See `security-remediation/sessions/SESSION-S3-12.md`. |
| S03-M2 | **Fixed** (S3-12, partial) | Verified | Tokens scope-bound, not uploader-bound | Claim re-confirmed before fixing: `verifyMediaToken()` only proves the caller participates in the chat/group the key belongs to (see `callerMayAccessScope()` at mint time) — it has no concept of *whose* media the key holds, so any participant could mint a genuine, correctly-signed write or delete token for a key another participant uploaded. **Fix in `worker/src/index.js`:** PUT now `head()`s the existing key before writing; if an object already exists with a recorded `customMetadata.uploader` that differs from the caller's token holder, the request is rejected 403 before the write happens (a brand-new key with nothing stored yet is unaffected — there is no prior uploader to protect on a first write). DELETE reuses the HEAD already performed for S03-H3's quota accounting to apply the identical check on the R2 (hot) tier; the B2 (cold) tier gets a new HEAD-before-DELETE step reading the migrated object's `x-amz-meta-uploader` header, which the R2→B2 migration cron now carries across from `customMetadata.uploader` specifically so this binding survives tiering — an object with no uploader tag at all (pre-fix migrations) is left deletable by any authorized holder rather than becoming permanently stuck. **Evidence:** `worker/src/index.test.js` — 8 new regression tests (non-uploader denied on PUT-overwrite/DELETE on both tiers; genuine uploader still succeeds on both tiers; first write to a new key is unrestricted; untagged legacy B2 object remains deletable) — `npm test` **17/17 pass** (9 pre-existing + 8 new); `node --check src/index.js` clean. See `security-remediation/sessions/SESSION-S3-12-partial.md`. **Follow-up session (S3-12, full):** S03-M1, S03-M3, and S03-L2 are now also **Fixed**/**Partial** — see their own rows. **Still Open, queued for a future S3-12 continuation:** S03-L3/S04-I2 (dead B2 presign surface), S10-N3 (cold-tier migration race). |
| S03-M3 | **Partial** (S3-12) | Verified | 10-min unrevocable bearer tokens, unlimited reuse | Claim re-confirmed: a capability token was a stateless bearer credential, replayable in full for its entire TTL by anything that observed the Authorization header (proxy log, crash report, etc.) — this was true for `read`/`write`/`delete` alike. **Fix in `worker/src/index.js` + `server/index.js`:** wire format grows a 6th segment, `jti` (`v1.<op>.<expiresAt>.<uidTag>.<jti>.<sig>`) — a random per-mint identifier (`crypto.randomBytes(9)`, base64url) included in the signed payload on both the mint side (`signMediaToken`) and verify side (`verifyMediaToken`); any token not exactly 6 dot-separated segments is now rejected as malformed, closing off the old 5-segment shape as a downgrade path. For `delete` specifically — the one verb with no undo — the Worker now marks a token's `jti` used in `RATE_KV` the first time it is actually consumed (`isTokenAlreadyUsed`/`markTokenUsed`, TTL'd to the token's own remaining lifetime) and 403s any subsequent replay of the exact same token, checked before touching R2/B2 at all. **Why Partial not Fixed:** TTL itself was intentionally left at its existing value (not "cut" per the finding's literal wording) — 10 minutes already matches the client's realistic mint-to-use window for a chat/voice message upload immediately after capture, and shortening it further was judged to risk legitimate-request failures without a corresponding security gain once single-use replay protection exists for the one irreversible verb; this is a deliberate scope trim, not an oversight. `read`/`write` tokens carry a `jti` for wire-format uniformity but are **not** tracked for reuse — re-fetching or re-uploading the same object within the TTL is normal client behavior, not something to block. The single-use guard is also KV-gated: without `RATE_KV` configured it silently degrades to "not enforced," the same documented best-effort characteristic as every other KV-backed control in this file (rate limits, quotas — see S03-I2). **Evidence:** `worker/src/index.test.js` — 3 new regression tests (a delete token can only be used once; two independently-minted delete tokens for different keys are each single-use independently; a legacy 5-part token is rejected as malformed) — `npm test` **27/27 pass**; `node --check src/index.js` and `node --check server/index.js` both clean. See `security-remediation/sessions/SESSION-S3-12.md`. |
| S04-M2 | **Fixed** (S3-08) | Verified | 24h redistributable TURN creds, no aggregate cap | Three independent gaps at `/turnCredentials`: (1) `ttl: 86400` hardcoded — 24h is far longer than a call needs and long enough that a bulk-minted/leaked credential (a plain bearer username/password valid from anywhere, bound to nothing) stays useful for a full day; (2) the 20/min per-account limiter still allowed ~28,800 mints/day with no ceiling above the minute window; (3) the outbound Cloudflare `fetch()` had no timeout, so a stalling upstream held the inbound+outbound sockets indefinitely. **Fix (`server/index.js`, `server/lib/pure.js`):** new pure `clampTurnCredentialTtlSeconds(requested, min, max)` clamps the TTL sent to Cloudflare into `[60s, 3600s]` regardless of env misconfiguration — the 3600s ceiling deliberately matches the Android client's own `TurnCredentialCache.TTL_MS` refresh assumption (`app/.../call/TurnCredentialCache.java`), so this changes nothing about how long a credential stays *useful* to the app, only how long a stolen one stays valid to anyone else; env-configurable via `TURN_CRED_TTL_SECONDS`. New `checkTurnDailyCap(uid)` layers a 24h/100-per-account cap (`TURN_CRED_DAILY_CAP` env var) on top of the existing per-minute limiter, reusing `pure.evaluateFixedWindow` with a 24h window instead of 60s, with its own hourly purge `setInterval` (mirrors the S02-L3 purge pattern). The Cloudflare `fetch()` is now wrapped in an `AbortController` (8s), mirroring `/linkPreview`'s existing pattern, and returns 504 on timeout instead of hanging. Also fixed the adjacent minor: `data.iceServers` is validated before responding 200 — an unexpected/missing shape now 502s instead of `JSON.stringify(undefined)` silently sending an empty 200 body. **Deferred, documented, not claimed fixed:** refusing to mint when the caller has no active call — `TurnCredentialFetcher#prefetch` runs *before* the `calls/{callId}` Firestore doc exists (see `CallManager.buildIceServers` javadoc), so gating on an active-call doc would break the legitimate prefetch flow without a coordinated client change; out of scope for a server-only fix. **Evidence:** `lib/pure.test.js` (+4 `clampTurnCredentialTtlSeconds` cases: caps above ceiling incl. the literal old 86400 default, raises below floor, passes through in-range, falls back to ceiling never the old 24h default on non-numeric input) — `npm test` 201/201 pass; `node --check index.js`/`pure.js` clean. |
| S04-M3 | **Fixed** (S3-07) | Verified | XFF trust hard-coded to one proxy | Claim re-confirmed before fixing: `getClientIp()` unconditionally trusted the rightmost `X-Forwarded-For` entry with no way to reconfigure for a different proxy topology (zero trusted proxies, or more than one). **Fix (`server/index.js` + new pure `pickClientIp()` in `server/lib/pure.js`, commit 000ed14):** `TRUSTED_PROXY_HOPS` env var (default `1`, reproducing the prior hardcoded behavior byte-for-byte on the current Render deployment) selects how many hops from the right are trusted; `0` disables XFF trust entirely and falls back to the raw socket address. **Evidence:** `lib/pure.test.js` (+6 `pickClientIp` cases: default-hop parity with old behavior, hops=0 ignores XFF, hops=2 second-from-right, insufficient-entries fallback, absent-header fallback, non-integer-hops default) — `npm test` 156/157 pass (1 pre-existing unrelated failure, see `sessions/SESSION-S3-07.md`); `node --check index.js` clean; `grep pure.pickClientIp index.js` confirms live wiring in `getClientIp()`. |
| S05-M1 | **Fixed** (S3-13) | Verified | Raw operator IPs/uids persisted. **Correction to the prior "Partial" disposition — source, not this session's own bias, disagreed with it.** The prior claim that `auditAdminEvent()` was "the single sink all 7 call sites funnel through" was false: `auditAdminEvent()` itself was correct (`ipTag(getClientIp(req))`), but **four other admin actions never called it** — `waitlist_approved`, `account_unfrozen`, `duress_enrolled`, `duress_revoked` each had their own `db.collection("adminAuditLog").add({ adminIp: getClientIp(req), ... })` written directly, bypassing the sink entirely and persisting the **raw** operator IP into the same permanent, no-TTL collection the sink exists to protect. Worse for `duress_enrolled`/`duress_revoked` specifically: the finding's own "most sensitive" callout (`action: "duress_enrolled", uid: <raw uid>`) was also still true — the `console.log` had been pseudonymised in S3-09, but the Firestore write next to it still carried the raw `uid`. **Fix this session:** all four call sites now route through `auditAdminEvent(action, req, extra)` (which already accepted arbitrary `extra` fields — no signature change needed), closing the raw-IP gap for all four; `duress_enrolled`/`duress_revoked` additionally pass `uid: uidTag(uid)` instead of the raw uid, matching the console.log redaction already in place; `account_unfrozen` deliberately keeps the raw uid (pre-existing, documented tradeoff — Firestore-access-controlled operator accountability record, unlike duress enrollment which the finding singles out). The last open item, `requestId=${requestId}` logged in the clear at `/requestAccess` and `/admin/api/waitlist/approve`, is also closed — both now log `reqTag(requestId)` via a new pepper-HMAC helper mirroring `ipTag`/`uidTag`. **Deliberately still not done:** a Firestore TTL/retention policy on `adminAuditLog` — the finding's own tension note resolves this as "pseudonymised-but-durable, not identifying," i.e. keep the audit trail durable (S05-H3's requirement) and make the identifying field non-identifying instead of deleting the record; adding a TTL here would trade away durability the finding itself says to keep. Regression tests: `adminAuditWiring.test.js` gained "no admin action writes adminAuditLog directly" (asserts exactly 1 `db.collection("adminAuditLog").add(` call site total — the sink's own definition), "duress enrollment/revocation audit rows never carry the raw uid", and "waitlist requestId is never logged to stdout in the clear". `npm test`: 197/198 pass (the 1 failure is the pre-existing `identityVerify.test.js` missing-native-module issue documented since S3-05, not a regression); `node --check index.js` clean. |
| S05-M2 | Fixed | Verified | `duressEligibility` enforced nowhere (paired with S06-M1) — see correction above. Re-verified (S3-10) from the same evidence as `S06-M1`: this is the identical underlying defect, "upgraded" to `S06-M1` per `audit/SESSION-06-DURESS.md:525` — one fix, one test suite, closes both IDs. |
| S05-M3 | **Fixed** (S3-13) | Verified | Admin sessions unbounded/unbindable. **Correction to the prior "Partial" disposition:** the claim that `ADMIN_SESSION_TTL_MS` (30 min) was already "an absolute TTL" was false — it was a pure sliding-refresh idle timeout with no ceiling at all: every `hasValidAdminSession(req)` call (including the unauthenticated `GET /admin` render check, invoked on every page load) reset `expiresAt` to `Date.now() + ADMIN_SESSION_TTL_MS`, so a session kept merely idling-open (e.g. an admin panel tab left open with periodic auto-refresh) never actually expired. There was also no IP/UA binding (a stolen `duoshield_admin_session` cookie worked unconditionally from anywhere) and no bulk-revoke (rotating `ADMIN_TOKEN` never touched the in-memory session map, so a session already minted survived a rotation indefinitely). **Fix — new `server/lib/adminSessionStore.js`** (pure, dependency-free, mirrors the `adminLockoutStore` factory-with-injectable-clock pattern): each session now tracks both `expiresAt` (idle, refreshed on real auth calls) and a separate `absoluteExpiresAt` set once at creation (`ADMIN_SESSION_ABSOLUTE_TTL_MS`, 8h) that a refresh can never push past — `validate()`'s refresh path is `Math.min(now + idleTtlMs, absoluteExpiresAt)`, so a session that is kept alive by continuous activity for its full 8h window is force-expired (`absolute_expired`) regardless of how recently it was used. Each session also stores the `ip`/`userAgent` bound at creation (`createAdminSession(req)` now takes `req` and passes `ip: ipTag(getClientIp(req))` — the same pseudonymised tag S05-M1 already writes to the audit log, not the raw IP — and `userAgent: req.headers["user-agent"]`); `validate()` rejects any request whose current context doesn't match with `ip_mismatch`/`ua_mismatch` **without deleting the session**, so the legitimate holder on the correct network/browser is unaffected while a replay from elsewhere is rejected. `GET /admin`'s unauthenticated render check now calls `hasValidAdminSession(req, { refresh: false })` so merely loading the page (not an authenticated admin/api action) can no longer extend a session's idle timeout — every genuinely authenticated call still refreshes as before. New `POST /admin/api/sessions/revoke-all` (admin-gated like every other `/admin/api/*` route) calls `adminSessionStore.revokeAll()`, clears the caller's own cookie in the response, and audits `admin_sessions_revoked_all`; a "Sign out everywhere" button was added to the admin panel next to the existing single-session "Sign out". `POST /admin/logout` now calls `adminSessionStore.revoke(sessionId)` (previously just `adminSessions.delete(sessionId)` against the same in-memory map — behaviorally equivalent, refactored onto the new store for consistency, not a prior gap). **Evidence:** `server/lib/adminSessionStore.test.js` — 15 new tests covering normal-session validity, refresh-before-expiry, refresh-capped-at-absolute-ceiling (asserted via the store's own `_get()` diagnostic accessor, not just the eventual rejection), IP-mismatch and UA-mismatch handling (both non-destructive), revoked-session rejection, `revokeAll()` bulk invalidation (including an empty-store no-op case), and legitimate-session continuity across multiple same-context refreshes. `server/lib/adminAuditWiring.test.js` gained 4 source-wiring checks (session creation binds `ip`/`userAgent`, `GET /admin` passes `{ refresh: false }`, revoke-all route is gated/calls-through/audits, logout calls `adminSessionStore.revoke`) plus `admin_sessions_revoked_all` added to the pre-existing required-audit-actions list. `npm test` (server): **216/217 pass** — the 1 failure is the same pre-existing unrelated `identityVerify.test.js` native-module issue (`Cannot find module '@signalapp/libsignal-client'`), not a regression; `node --check index.js` clean. See `sessions/SESSION-S3-13.md`'s "S05-M3" section for the full trace of every session creation/validation/refresh/logout/revocation path read before editing. |
| S06-M2 | Fixed | Verified | `_duressNonces` unbounded growth — **tracker was stale, not the code.** `firestore.indexes.json` declares a `ttl: true` field-level TTL policy on `_duressNonces.expiresAt` (Firestore's own delete-when-expired mechanism); `/requestLockNonce` (`index.js:3512-3522`) also deletes any prior nonce doc(s) for that uid before issuing a new one, so a uid can hold at most one outstanding nonce belt-and-braces on top of the TTL; the issuance window was also shortened from 24h to 1h (`:3534`). Re-verified from source this session, no new code required. |
| S06-M3 | Fixed | Verified | Raw uids logged on duress endpoints — `requestLockNonce`/`duress-lock` already used `uidTag()`; the two remaining un-redacted lines this finding overlapped with S05-M1 on (`/admin/api/duress/enroll` and `/admin/api/duress/revoke`, `index.js:4112`/`:4159` prior to this session) now use `uidTag(uid)` too. `grep -n 'uid=\${uid}\`' server/index.js` returns no hits — no raw-uid `console.*` log lines remain anywhere in the file. |
| S06-M6 | **Fixed** (code landed `557bdbc`; tracker row added and residual UX dead end + test coverage closed in a later session) | Verified by source read; **not build-verified** — no JDK/Gradle/Android SDK in this review context, so the Java side was checked by manual review (brace/paren balance, import presence, call-site grep, layout-ID cross-check against `findViewById`), not compilation. Server side: `node --check server/index.js` clean, and `node --test server/lib/pure.test.js` executes (76/76 pass, including the 7 new `decideRotationAcknowledgement` tests). | Unfreeze after a duress wipe returned the account to service on its **pre-wipe credentials** — no forced rotation | `/admin/api/locked/unfreeze` writes `locked:false` **plus** `rotationRequired:true` on `accountLock/{uid}`, but nothing consumed the second flag: the account came back usable on whatever primary PIN and slot-B duress code existed before the wipe, i.e. exactly the credentials that were plausibly coerced out of the user in the first place. **Why the client can't clear it itself:** `firestore.rules` denies `accountLock` update outright (`allow update: if false`), so the flag can only be cleared by an Admin-SDK endpoint — the same reason `/migrateUid` exists. **Fix — 2-screen mandatory chain, discovered on the one path that can see the flag.** `RestoreFromSeedActivity` (`:296`) reads `rotationRequired` off the `lockDoc` it *already* fetches for the `locked` check (no extra read); a duress wipe destroys the local session, so restore-from-seed is the only way back into the account and therefore the sole client-side consultation point. At the routing tail (`:509-520`) rotation is checked **before** the S06-M?/Item-4 `!PinManager.hasPinSet()` fallback — deliberately, because a device PIN promoted a few lines earlier (`:493`) would otherwise satisfy `hasPinSet()` and let the account walk straight past the mandatory rotation into the app. Screen 1 `ForcedPrimaryPinRotationActivity` forces a fresh primary PIN (P2) with `SetupPinActivity`'s clash guard (`DuressManager.isDuressPinOrPrefixOfIt`) and its off-main-thread PBKDF2 shape; screen 2 `ForcedDuressRotationActivity` re-arms slot B, reusing `ManageUnlockCodesActivity`'s confirm-current-PIN + bidirectional-prefix-overlap rejection — confirming against P2 rather than transporting P2's plaintext across the Activity boundary, per the codebase's no-persisted-plaintext posture. Both override `onBackPressed()` to a no-op and expose no skip path. **Crash/kill resilience:** `PendingLockStore` gained four keys (`KEY_ROTATION_DUE`, `KEY_ROTATION_PRIMARY_DONE`, `KEY_ROTATION_DURESS_DONE`, `KEY_ROTATION_UNPAIRED`) in the wipe-surviving encrypted `SecurePrefs.getSessionState` container, all `commit()`-not-`apply()` like the rest of that file; `MainActivity.route()` (`:134-152`) resumes at the correct screen ahead of the existing `pending_pin_setup_` self-heal block. `KEY_ROTATION_DURESS_DONE` is deliberately a **separate flag rather than a `DuressManager.hasDuressPin()` check** — slot B can already be armed on first arrival at screen 2 (duress logout keeps the old code armed so a restore of the same account stays gated), so "armed" alone cannot distinguish "screen 2 hasn't run" from "screen 2 finished and only the ack is outstanding," and reading it directly would have skipped a fresh visitor straight to the ack-retry panel with their coerced code left in place — the exact defect this finding is about. `KEY_ROTATION_UNPAIRED` persists the final destination (`AddContactActivity` vs `ConversationListActivity`) rather than forwarding an Intent extra, so a mid-flow process death doesn't lose it. **Server — `POST /acknowledgeRotation` (`server/index.js:2937-3051`), modeled on `/migrateUid`:** Bearer ID token → `verifyIdToken` → `checkAuthRateLimit(uid, "acknowledgeRotation")` (new `AUTH_RATE_LIMITS` entry, 3/60s — one-shot completion call, same reasoning as `requestLockNonce`) → `decodedToken.uid !== userId` ⇒ 403 → transaction on `accountLock/{userId}`. Missing doc ⇒ `{acknowledged:false,reason:"no-lock"}` 200 and `rotationRequired !== true` ⇒ `{…reason:"not-due"}` 200, both **idempotent on purpose** so a retry after a successful-but-unconfirmed first attempt doesn't strand the user on a screen with no exit. `locked === true` ⇒ **403**, checked before the not-due branch: if the account was re-locked since the unfreeze, a client still mid-flow from the earlier unfreeze must not clear anything. Never touches `locked` itself; logs via `uidTag()` (S06-M3/S05-M1 discipline) with no `adminAuditLog` entry — routine client self-service, not an operator action. **Ordering that matters:** local codes are saved *before* the ack is attempted, and `clearRotationDue()` runs *only* on a 2xx — an ack failure leaves both new codes durably set and both flags set, so the retry panel is the resume point and no failure path can discard a credential the user has already committed to. **No `firestore.rules` change** (Admin SDK bypasses rules) and **no ordinary-sign-in routing change** (rotation is unreachable except via restore-from-seed). **Residual UX dead end — closed this session.** The 403-on-relocked case used to surface as the same generic retryable network error as a real connectivity failure, leaving the user in a retry loop with no exit. `ForcedDuressRotationActivity` now distinguishes that response with a dedicated `RotationDeniedException` — a 403 from this endpoint is unambiguous, since the token-UID-mismatch branch (the only other 403) can never fire here (`userId` is always `user.getUid()` from the same token being sent) — and `exitRotationDenied()` signs out through `BaseActivity.KEY_EXPLICIT_SIGNOUT` and lands on `SignInActivity`, the same generic outcome as any other sign-out. This adds no new copy and leaks nothing new: lock state is still discoverable only through the operator's own unfreeze action, exactly as before; the fix is that a dead end no longer *looks like* a transient failure worth retrying forever. Deliberately leaves `PendingLockStore`'s rotation flags set — harmless, since a future unfreeze sets `rotationRequired` fresh regardless, and `MainActivity.route()` never consults them without a signed-in session. **Regression coverage — added this session.** `/acknowledgeRotation`'s branch logic (no-lock / relocked / not-due / due) is extracted to `decideRotationAcknowledgement` in `server/lib/pure.js`, following the S06-L1 pattern, with 7 tests in `server/lib/pure.test.js` (`node --test`: 76/76 pass in that file, 269/270 across the whole suite — the one pre-existing failure is in `identityVerify.test.js`, untouched by this change). The Java side still has no instrumentation harness in this repo, so `RotationDeniedException`/`exitRotationDenied()` are verified by source read only (brace/paren balance, import/class-visibility checks), not by an executed test. |
| S07-M2 | **Partial** (S3-17) | Verified | Trust keyed on mutable uid — `DuoShieldSignalStore.java` stores trust under `signal_trusted_id_<uid>` | Claim re-confirmed before fixing: `saveIdentity`/`getIdentity`/`isTrustedIdentity` all keyed the trust record on `signal_trusted_id_<address>` — address embeds Firebase's mutable uid, so a reassigned/re-provisioned uid could silently inherit whatever trust was recorded for "this uid", with no binding to the actual cryptographic identity. **Fix:** trust is now anchored on `fingerprintOf(serializedKey)` (SHA-256 of the serialized identity key, immutable) via two new SecurePrefs key families — `KEY_FP_POINTER_PREFIX + address` (cache: which fingerprint currently answers for this address) and `KEY_TRUST_BY_FP_PREFIX + fingerprint` (the actual trust record). New `readTrustRecord(address)` is a one-time, read-triggered migration: on first read for an address with no fingerprint-keyed record yet, it reads the legacy uid-keyed record (if any), computes that same key's fingerprint, and writes it forward into the new storage — it migrates the address's **own already-trusted key**, not a different one, so no cross-uid trust misattribution is introduced; a genuinely first-contact address still returns "no record" exactly as before. Legacy `KEY_TRUSTED_IDENTITY_PREFIX` key is left in place afterward (harmless, unread going forward). `saveIdentity`/`getIdentity`/`isTrustedIdentity` all route through `readTrustRecord`. **Gap:** no new dedicated unit test was added for the migration path itself this session (the change is exercised only via the existing `Log.w`/exception-path review, not a JUnit test) — noted as a residual, not fabricated as covered. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — source-reviewed and structurally checked, not compiled/tested; AND-lane verification routed to **S3-19b**. See `SESSION-S3-17.md`. |
| S07-M3 | **Partial** (S3-17) | Verified | Backup metadata outside the AEAD | Claim re-confirmed before fixing: `BackupCryptoHelper.encryptCompressed`/`decryptCompressed` authenticated only the ciphertext itself — the doc's own id, `conversationId`, and the `compressed` flag lived as plain, unauthenticated Firestore fields alongside `enc`, so flipping `compressed` or splicing one document's ciphertext under a different id/conversationId would not fail decryption. **Fix (same commit as S07-H2):** new `BackupCryptoHelper.buildAad(docId, conversationId, compressed)` + AAD-aware `encryptCompressed`/`decryptCompressed` overloads (`Cipher.updateAAD`); `BackupManager.java`'s 4 write call sites now build and pass this AAD, and the restore path reconstructs the identical AAD from the doc's own Firestore fields **only** when the doc carries the new `hmac` field (the marker for "written by the current, AAD-aware path") — a doc written before this fix has no bound AAD and keeps decrypting exactly as it always did. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — source-reviewed and structurally checked, not compiled/tested; AND-lane verification routed to **S3-19b**. See `SESSION-S3-17.md`. |
| S08-M1 | **Partial** (S3-19) | Verified | Native heap pointer tagging disabled — `AndroidManifest.xml:54` `allowNativeHeapPointerTagging="false"` | **Fix (`AndroidManifest.xml`, commit `51f5573`):** deleted the attribute entirely — its absence restores the platform default of `true`, re-enabling pointer tagging (Android 11+, arm64 TBI) as a memory-safety mitigation for the three native libraries this process loads that parse attacker-controlled bytes (libsignal's Rust core, WebRTC media parsing, SQLCipher). The finding's fix also said "if a library genuinely faults, identify it, file it upstream, and record the specific reason inline" — that step needs an actual device/emulator run, the same toolchain blocker as everything else this round; if S3-19b's real build+test pass surfaces a genuine fault from one of these libraries, the attribute should be reinstated with an inline comment naming the specific library, not restored unconditionally. Lands **Partial**, not Fixed: source-reviewed, `xmllint --noout` confirms the manifest is still well-formed after the removal, not compiled/run — **S3-19b**. |
| S08-M2 | **Partial** (S3-19) | Verified | FileProvider root-scoped grantable paths — `file_paths.xml` still declares whole-root `cache-path`/`files-path`/etc. | Claim re-confirmed before fixing: all four declared roots (`cache-path`, `external-cache-path`, `files-path`, `external-files-path`) were scoped to `path="."` — the entire directory. Grep confirmed the app never calls `getExternalFilesDir`/`getExternalCacheDir` anywhere, and the only `getFilesDir()` uses (`SettingsActivity`'s avatar copy, `B2StorageHelper`'s disk cache) are never passed to `FileProvider.getUriForFile()` — only the 4 genuinely-shared files (2 camera captures, 1 shared image, 1 export ZIP, all previously written straight to the cache root) ever got a grant, and every grant was, in principle, scoped to the whole cache directory including S08-H3's plaintext Glide disk cache. **Fix (commit `51f5573`):** new `util/SharedCacheDir.java` centralizes one FileProvider-grantable root, `getCacheDir()/shared/`, split into `camera`/`media`/`export` subdirectories by purpose; `file_paths.xml` now declares only `<cache-path name="shared" path="shared/" />` — the other three roots (both external roots and the unscoped `files-path`) are deleted outright, not just narrowed. `ChatMediaActivity`/`GroupChatActivity` (camera captures), `SecureShareHelper` (shared image), and `ChatExportHelper` (export ZIP) now create their FileProvider-shared files under `SharedCacheDir`'s subdirectories instead of the cache root. **Interaction with S08-H3 handled:** `TempFileCleaner` only ever swept `getCacheDir()`'s top level, so relocating these 4 file classes into `shared/**` would have silently stopped them from being cleaned up (reopening S08-H3) had the sweep not been extended — `TempFileCleaner.sweepDir()` was extracted from the existing per-file loop and is now called a second time over `getCacheDir()/shared/<category>/` for each category subdirectory, applying the identical filename/age rules already used at the cache root. Lands **Partial**, not Fixed: source-reviewed; `xmllint --noout` confirms `file_paths.xml` is still well-formed; brace/paren/bracket balance PASS on all 7 touched `.java` files; not compiled/run (no JDK/Gradle/Android SDK) — **S3-19b**. New regression coverage: `S319ManifestTest.java` (plain JVM XML-parse test, no Android SDK needed) asserts `file_paths.xml` declares exactly one root (`cache-path name="shared" path="shared/"`) and that all three removed roots are gone; also asserts `<application>` carries no `allowNativeHeapPointerTagging` attribute for S08-M1. Both assertions were hand-verified against the live files with a Python XML parser (standing in for JDK DOM behavior, including the namespace-declaration mismatch between the two files — `AndroidManifest.xml` declares `xmlns:android` and is parsed namespace-aware via `getAttributeNS`, while `file_paths.xml` declares no such namespace and its `name`/`path` attributes are read unprefixed, matching FileProvider's own documented meta-data format) before being written, but the test itself has not been compiled/run. |
| S08-M3 | **Accepted** (S3-20) | Verified | No root, tamper, hooking, or emulator detection, and no keystore attestation | Re-verified this session: repo-wide grep for `RootBeer`/`isRooted`/`isEmulator`/`FridaDetect`/`isInsideSecureHardware` across `app/src/main/java` returns nothing — the gap is unchanged from the audit's own finding. **Rationale (per `audit/SESSION-08-CLIENT-PLATFORM.md`):** the threat model already treats client-side integrity checks as advisory, not controls, since the audit's own attacker model grants the attacker the client; adding root/hooking detection would not close any trust boundary, only raise the bar for a class of attacker the model does not defend against. **Accepted, not fixed, because:** this is a genuine capability gap, not a false alarm — the audit is explicit that root/hooking detection is the only signal that could inform three *other* still-open findings (`S08-H5`/`S07-M1`'s plaintext-fallback silent readability on a rooted device, `S08-H3`'s plaintext Glide cache, and `S06-I3`'s hardware-backing question) and that `KeyInfo.isInsideSecureHardware()` is the specific missing call. **Residual risk (unchanged from audit, not newly assessed):** on a rooted/hooked/emulated device, `PinManager`'s PBKDF2 verify, the duress-PIN comparison, and the local lock timer can be hooked and always return success with no signal to the user, and a repackaged APK (no `PackageManager` signature check) is indistinguishable from the genuine one. **Operational dependency:** none from this session; the audit's own priority order (`KeyInfo.isInsideSecureHardware()` first, then root/signature checks) is a future-session implementation decision, not something S3-20 closes. |
| S10-N1 | **Partial** (S3-15) | Verified | Firebase App Check absent | Claim re-confirmed before fixing: repo-wide grep for `appcheck`/`App Check`/`playintegrity` returned nothing at all — no dependency, no provider registration, no rules-side check. **Fix (commit 9b58b3a):** `app/build.gradle` adds `firebase-appcheck-playintegrity` (release+debug) and `firebase-appcheck-debug` (`debugImplementation` only, so it never ships in a release APK); `DuoShieldApp.onCreate()` installs `PlayIntegrityAppCheckProviderFactory` (+ `DebugAppCheckProviderFactory` under `BuildConfig.DEBUG`) before any other Firebase SDK call in the method, wrapped in try/catch that fails open (logs, does not crash) if install fails on a device/Play-Services combo that can't attest; `firestore.rules` adds an `appCheckVerified()` helper (`request.app != null`). **Deliberately not wired into any `allow` clause** — per the audit's own recommendation (`audit/SESSION-10-SYNTHESIS.md` §S10-N1) to roll out in monitoring mode first, and because every existing Android install needs the App-Check-aware APK before any rule could require `request.app` without locking out real traffic, and `firestore-tests/rules.test.js`'s emulator harness does not currently mint App Check tokens (would fail every existing case). Enabling enforcement is a two-part **operator** step, tracked, not executed here: (1) Firebase console → App Check → Firestore/Storage → switch from monitoring to "Enforce"; (2) add `&& appCheckVerified()` to the `allow` clauses this finding calls out once console metrics confirm real traffic is attaching tokens. Sideloaded/rooted-device installs remain an **accepted** residual gap regardless (README.md's own threat model: a compromised client bypasses attestation by definition — App Check raises attacker cost, it is not a trust-boundary control). **Why Partial not Fixed:** AND lane (Android compile) is still source-review only, pending **S3-19b**. **RULES lane update (S3-15b, this session):** installed Java 21 + firebase-tools and ran the full `firestore-tests` suite against the live emulator (186/186 pass) — this confirms the `appCheckVerified()` helper's addition did not break any existing rule and that the emulator harness itself runs clean, but since the helper is deliberately **not wired into any `allow` clause** yet (monitoring-mode rollout, by design), there is no App-Check-gated behavior for the emulator to actually exercise — S3-15b closes the "does the ruleset still work" question, not an App-Check-specific test. Promotion to `fixed` still routes through **S3-19b** for the AND half. **Evidence:** `grep AppCheck` confirms the 3 new import/usage sites in `DuoShieldApp.java` and 2 dependency lines in `build.gradle`; `firestore.rules` brace balance 79 open / 79 close (Python count, this session); `cd server && npm test` 229/230 pass (1 pre-existing `identityVerify.test.js` native-module failure, not a regression — server code was not touched by this session, confirms no collateral breakage). |
| SC-07 | **Fixed** (S3-03) | Verified | Wrapper JAR unvalidated — no `gradle/wrapper-validation-action` in any workflow | Claim re-confirmed: no wrapper validation ran anywhere. **Fix in `ci.yml`:** new `validate-gradle-wrapper` job runs `gradle/actions/wrapper-validation` (SHA-pinned) to check the committed `gradle-wrapper.jar` against official Gradle release checksums. Added to `lint.needs` alongside `verify-libsignal-jar`, so — as `lint` is the fan-in prerequisite of every build job — a tampered wrapper JAR fails the whole pipeline before `./gradlew` ever executes. **Evidence:** `js-yaml` parse OK; `validate-gradle-wrapper` present in jobs; `lint.needs=["verify-libsignal-jar","validate-gradle-wrapper"]`. |
| SC-08 | **Fixed** (S3-03) | Verified | Actions on mutable tags — `actions/checkout@v4` etc. still tag-pinned, not SHA-pinned | Claim re-confirmed: every `uses:` across all workflows referenced a mutable tag. **Fix:** SHA-pinned every third-party action to a full 40-char commit SHA (resolved live via `gh api repos/<a>/commits/<tag>`) with a trailing `# vN` comment for readability, across `ci.yml`, `release.yml`, `firestore.yml`, `firestore-rules-test.yml`, and the new `security-scan.yml` — e.g. `actions/checkout@11d5960a…262 # v4`, `gradle/actions@ed408507…3a # v4`, `actions/attest-build-provenance@e8998f94…be # v2`. **Evidence:** repo-wide `grep -E "uses: [^@]+@v[0-9]+$"` under `.github/` → **no matches** (zero unpinned actions remain); all workflows parse under `js-yaml`. |
| SC-09 | **Fixed** (S3-03) | Verified | No scanning/SBOM/Dependabot | Claim re-confirmed: no `.github/dependabot.yml` and no scanning workflow. **Fix (two files):** (1) `.github/dependabot.yml` enables weekly updates for `github-actions` (`/`), `gradle` (`/`), and `npm` for both `/firestore-tests` and `/server`, with grouped minor/patch PRs and open-PR caps. (2) `.github/workflows/security-scan.yml` (all actions SHA-pinned, least-priv `permissions`) runs on push/PR/weekly-cron: **CodeQL** SAST for `java-kotlin` + `javascript-typescript`, **gitleaks** secret scanning, and **SBOM** generation via `anchore/sbom-action` uploaded as an artifact. **Evidence:** `js-yaml` parse OK for both; dependabot ecosystems enumerated (`github-actions:/`, `gradle:/`, `npm:/firestore-tests`, `npm:/server`); security-scan jobs = `codeql,gitleaks,sbom`. Actual scan runs are operator-side (need a push/PR event), per the CI lane. |
| SC-10 | **Fixed** (S3-03) | Verified | Firestore deploy runs unpinned `npm install` | Claim re-confirmed: `firestore.yml` + `firestore-rules-test.yml` used bare `npm install` and `npm install -g firebase-tools` (floating latest). **Fix:** both workflows now use `npm ci` (installs exactly from the committed `firestore-tests/package-lock.json`, failing if manifest and lock disagree) and pin the global CLI to `firebase-tools@15.26.0`. **Evidence:** grep confirms `npm ci` (no bare `npm install`) and `firebase-tools@15.26.0` in both workflows; `firestore-tests/package-lock.json` present for `npm ci` to consume; `js-yaml` parse OK. |

## Low (33)

| ID | Status | Confidence | Issue |
|---|---|---|---|
| S01-L1 | **Fixed** (S3-05, emulator-verified S3-15b) | Verified | `groups` create `createdBy` validation — closed incidentally by the S03-H1 fix (`createdBy == auth.uid`, `createdBy in members`, no collision with an existing chat); full shape validation still absent | The ID-squatting/namespace-collision half was already closed by S03-H1 (`!exists(chats/$(groupId))`). The remaining gap — no shape validation on the fields a client actually sets at create time — is fixed here. **Fix (`firestore.rules`, commit 3070b0b):** create now additionally requires `keys().hasOnly(['name','createdBy','createdAt','members'])`, `name is string` sized 1–200, `members is list` sized 1–256 (grepped `CreateGroupActivity` — every `collection("groups").document(...).set(...)` call site writes exactly this field set; `lastActivity` is only ever set later via `update`, never at create). **S3-15b (this session):** ran the 4 shape tests (extra field → fail; empty name → fail; non-string name → fail; 257-member list → fail) plus the pre-existing legitimate-create tests against the live Firestore emulator — all passed, part of the 186/186 full-suite run. Promoted from Partial to Fixed per the S3-15b gate. |
| S01-L2 | **Fixed** (S3-05, emulator-verified S3-15b) | Verified | `users` doc write has no field/shape validation | Claim re-confirmed: `write` (create+update+delete) let the owner put arbitrary fields/values into a doc every authenticated user can read — same content-injection class as S01-M2 but on `users/{uid}`. **Fix (`firestore.rules`, commit 3070b0b):** split `write` into `create, update` (allow-listed: `displayName`/`fcmToken`/`platform`/`photoUrl`/`updatedAt`, each with a type/size bound — grepped every `collection("users").document(...)` call site in `ContactManager`/`SettingsActivity`/`FcmTokenHelper`/`SeedPhraseDisplayActivity`) and `delete` (unconstrained, matching prior "owner can write" semantics for an operation no client code performs — `request.resource` doesn't exist on delete so it can't be shape-checked anyway). **S3-15b (this session):** ran the 4 tests for this finding (all allow-listed fields → succeeds; extra field → fail; oversized displayName → fail; non-string fcmToken → fail) against the live Firestore emulator — all passed, part of the 186/186 full-suite run. Promoted from Partial to Fixed per the S3-15b gate. |
 | S02-L1 | **Fixed** (S3-06, pre-existing) | Verified | Mint hash check fails open (= S07-H1) | Same defect and fix as `S07-H1` above — one fix closes both. Fixed in commit `5c2cd73` (predates this tracker); re-verified from source this session, tracker corrected, no code change made. |
 | S02-L2 | **Fixed** (S3-06) | Verified | `createChat` stores unbounded/unsanitized display names | Claim re-confirmed before fixing: `myDisplayName`/`partnerDisplayName` were written to the chat doc straight from the request body with no type check or length bound — any authenticated caller could stash an oversized or non-string value on a doc the other participant reads. **Fix (`server/index.js` + `server/lib/profileSanitize.js`, commit 269d165):** `isValidDisplayName()` requires a non-empty string ≤200 chars (matching the `users/{uid}.displayName` rule bound) before either name is persisted; invalid values are silently omitted rather than stored. **Evidence:** `lib/profileSanitize.test.js` (4 cases: normal name accepted, non-string rejected, empty string rejected, exact-boundary/one-over-boundary length) — `npm test` 146/147 pass (1 pre-existing unrelated failure, see S02-H1 note); `node --check index.js` clean; `grep isValidDisplayName index.js` confirms live wiring at the `/createChat` call site. |
| S02-L3 | **Fixed** (S3-07) | Verified | `mintCooldown` map only purges an entry once a request lands with `last === 0`; otherwise grows per active user | Claim re-confirmed before fixing: unlike `ipHits`/`waitlistIpHits`/`authRateLimits`, `mintCooldown` had no periodic purge �� one entry accumulates per distinct `userId` ever seen (`userId` is not a secret, so an attacker can grow it arbitrarily) and is never removed on a long-lived Render instance. **Fix (`server/index.js` + new pure `collectStaleKeys()` in `server/lib/pure.js`, commit 000ed14):** a 5-minute `setInterval` purges any entry older than the 60s cooldown window, matching the cadence of the sibling limiters. **Evidence:** `lib/pure.test.js` (+4 `collectStaleKeys` cases: mixed fresh/stale, exact-TTL-boundary not-yet-stale, works directly against a live `Map`, empty input) — `npm test` 156/157 pass (1 pre-existing unrelated failure); `node --check index.js` clean; `grep pure.collectStaleKeys index.js` confirms live wiring. |
| S02-L4 | **Fixed** (S3-07) | Verified | `collectBody` counts chars (`body.length`), not bytes — no `setEncoding` call | Claim re-confirmed before fixing: `collectBody()` capped `body.length` — the JS string's UTF-16 code-unit count after `body += chunk` implicitly calls `chunk.toString("utf8")` — instead of the true wire byte count. A 4-byte UTF-8 sequence (e.g. many emoji) decodes to 2 UTF-16 code units, so an attacker sending such bodies could push roughly double `MAX_BODY_BYTES` (64 KB → ~128 KB) onto the heap before the 413 ever tripped. No `setEncoding()` call exists on `req` anywhere (confirmed by repo-wide grep) — chunks are always Buffers, so this was purely a counting bug, not an encoding-mode bug. **Fix (`server/index.js`, commit 000ed14):** `collectBody()` now tracks `bytes += chunk.length` (Buffer byte length) exactly like the already-correct `readBody()` a few lines above, and compares `bytes > MAX_BODY_BYTES`, not `body.length`. **Evidence:** `npm test` 156/157 pass (1 pre-existing unrelated failure); `node --check index.js` clean; `grep "bytes > MAX_BODY_BYTES" index.js` shows both `readBody` and `collectBody` now using the identical byte-tracked check. |
| S03-L1 | **Fixed** (S3-01) | Verified | `WORKER_SECRET` compiled into APK (= S08-H1) — closed with S08-H1 this session; duplicate finding, same evidence |
| S03-L2 | **Fixed** (S3-12) | Verified | Unguarded `decodeURIComponent` | Claim re-confirmed before fixing: `decodeURIComponent(url.pathname.slice(1))` ran unguarded as the very first input-handling step in the fetch handler — a malformed percent-escape (e.g. a lone `%` or `%zz`) throws `URIError`, which was uncaught and fell through to a generic 500/1101 instead of the normal 400 this file returns for every other malformed-input case. No state or information was at stake, but this was the one control in the request path that ran before every other check, unguarded. **Fix in `worker/src/index.js`:** wrapped the decode in a `try`/`catch` that returns the standard `{ error: 'Invalid file key' }` 400 response on failure, matching the shape of every other malformed-input rejection in this handler. **Evidence:** `worker/src/index.test.js` — 2 new regression tests (`%zz` and a lone trailing `%` in the path each 400, not an uncaught exception) — `npm test` **27/27 pass**; `node --check src/index.js` clean. See `security-remediation/sessions/SESSION-S3-12.md`. |
| S03-L3 | **Fixed** (S3-12) | Verified | Dead B2 presign code (= S04-I2) — closed with S04-I2 this session; duplicate finding, same evidence |
| S03-L4 | **Fixed** (S3-11) | Verified | Rejections without CORS headers — global CORS headers are applied via a shared helper, but not confirmed on every quota-rejection path | **Fix in `worker/src/index.js`:** `json()` now takes an `extraHeaders` param merged into every response; the fetch handler computes `cors = corsHeaders(request, env)` once and closes over it in a local `respond(data, status) => json(data, status, cors)` used for every success and error path (400/401/403/404/413/429/502/507). Quota/rate-limit rejection helpers (`checkDailyRequestLimit`, `checkPerUserRateLimit`) now take `cors` as a parameter and include it on their 429 responses. The one non-`respond()` path (`/stats` 401) spreads `...cors` directly. **Evidence:** `grep -n "new Response|respond(|corsHeaders(" src/index.js` shows no bare `corsHeaders()` call site outside the shared `cors` variable — every response construction goes through `respond()`/`json()` or explicitly spreads `cors`; `npm test` 9/9 pass. See `security-remediation/sessions/SESSION-S3-11.md`. |
| S04-L1 | **Fixed** (S3-07) | Verified | `collectBody` byte count / `setEncoding` (= S02-L4) | Same defect and fix as `S02-L4` above — one fix closes both. Commit `000ed14`. |
| S04-L2 | **Fixed** (pre-existing, S3-08) | Verified | `/duress-lock` no rate limit (= S06-L2) | Re-verified from source this session, per SESSION_PROTOCOL §3 ("source beats tracker") — stale-`open` in the tracker but already remediated. `server/index.js:3481-3487`: `checkIpRateLimit(clientIp)` gates the handler's first line, 429s and logs on rejection. See `S06-L2` for the paired detail (same code, one fix closes both rows). Not reimplemented, not re-tested — no new code, tracker corrected in place. |
| S04-L3 | **Fixed** | Verified | Limiter state per-process/in-memory | Scoped to the highest-value target: the admin brute-force failure counter (`adminIpFails`, gating `ADMIN_TOKEN` guessing) reset on every restart/redeploy and was not shared across instances. **Fix (commit `fe9559a`):** new `server/lib/adminLockoutStore.js` backs `isLocked()`/`recordFailure()`/`reset()`/`count()` with Upstash Redis (`KV_REST_API_URL`/`KV_REST_API_TOKEN`, already-provisioned integration) — `recordFailure()` uses a single Lua `EVAL` (INCR + conditional PEXPIRE) so two concurrent failing requests can't race the "first failure sets the TTL" branch and repeatedly extend the window; falls back to the pre-fix in-memory Map (same semantics, not silently presented as durable — `onError()` warns on every fallback) only if Redis is unconfigured or a call throws. `server/index.js`'s `adminIpLocked()`/`recordAdminAuthFailure()` now delegate to the store and are `async`; a new `resetAdminAuthFailures()` clears an IP's failures on successful login (previously they only ever accumulated). IP normalization is injected from `pure.normalizeIpForRateLimit` so the S04-M1 fix applies to the durable store too. **Not in scope:** the other in-memory limiters (`ipHits`, `waitlistIpHits`, `waitlistPollHits`, the challenge/nonce stores) remain process-local — lower-value targets than the admin gate, left open for a future pass if warranted. **Evidence:** `server/lib/adminLockoutStore.test.js` (13 cases, mocked Redis client, no live Upstash needed) — local-fallback accumulate/lockout/reset, Redis-backed accumulate via the atomic script, TTL anchored on first failure only, TTL expiry, reset-on-success, Redis-error fail-safe on all three ops, IPv6/64 key normalization. `npm test` (server/): 194/194 pass (was 181/181 pre-fix). `node --check` clean on `index.js`, `adminLockoutStore.js`, `adminLockoutStore.test.js`. **FOLLOW-UP (2026-08-14): migrated Upstash Redis → Cloudflare Workers KV.** `adminLockoutStore.js` now takes a `kv` client (`server/lib/cloudflareKvStore.js`, a small dependency-free Cloudflare REST client) instead of a Redis client, reading `CLOUDFLARE_ACCOUNT_ID`/`CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_KV_NAMESPACE_ID` (`KV_REST_API_URL`/`KV_REST_API_TOKEN` removed from `index.js` and `server/package.json`'s `@upstash/redis` dependency dropped). Cloudflare KV has no atomic INCR/EVAL equivalent, so `recordFailure()` is now a non-atomic GET→increment→PUT — a deliberate, documented tradeoff (see `adminLockoutStore.js`'s "MIGRATION" note) accepted because the counter only bounds guesses against a 128-bit-floor `ADMIN_TOKEN`, matching this project's existing precedent of non-atomic KV counters in `worker/src/index.js`. The window's start time is now stored in the record itself (`{count, windowStart}`) rather than relied on from KV's own TTL, since Cloudflare's `expiration_ttl` has a 60s floor and no "leave the existing TTL alone" write mode. **Evidence:** `server/lib/adminLockoutStore.test.js` rewritten for the KV shape (missing-key, retrieval, TTL/window-anchoring, expiry, fresh-window-after-expiry, reset, KV API errors, malformed stored values, network timeout, and a concurrent-recordFailure test that locks in "undercount possible, never overcount or throw" as the accepted race); new `server/lib/cloudflareKvStore.test.js` (17 cases: URL/method/auth-header construction, 404-vs-error distinction, TTL clamping to the 60s floor, `success:false` 200 responses, malformed bodies, network failure, abort-on-timeout, token never appears in any thrown error). `npm test` (server/): 270/270 pass. `node --check` clean on `index.js`, `adminLockoutStore.js`, `cloudflareKvStore.js`, and both test files. **FOLLOW-UP #2 (2026-08-14, same day): the KV migration above was rejected before deploying and replaced with a Cloudflare Durable Object.** The non-atomic GET→increment→PUT race was correctly flagged as a real security regression for a brute-force counter, not an acceptable KV limitation — under concurrency it could undercount the lockout and widen an attacker's guess budget beyond the intended 10. `cloudflareKvStore.js` is deleted entirely (see git history for the removed version). The store now calls `server/lib/adminLockoutWorkerClient.js`, a thin HTTP client to a new `/adminLockout` route on the project's *existing* Cloudflare Worker (`worker/src/index.js`, already deployed for tiered media storage — no new Worker was created), which forwards to a new Durable Object class (`worker/src/adminLockoutDurableObject.js`, `AdminLockoutCounter`). Cloudflare guarantees all requests to one Durable Object instance (one per normalized IP, via `idFromName`) are processed strictly one at a time, so the increment-and-check is genuinely atomic — restoring the same guarantee the original Redis `EVAL` had, on the free Workers plan (Durable Objects with the SQLite storage backend, declared via `new_sqlite_classes` in `wrangler.jsonc`, do not require a paid plan). Auth uses a new, separate secret (`ADMIN_LOCKOUT_SECRET` on the Worker / `ADMIN_LOCKOUT_WORKER_SECRET` on Render) rather than reusing `STATS_SECRET`, since one is a server-to-server credential and the other an operator bearer token for different trust boundaries. Env vars are now `ADMIN_LOCKOUT_WORKER_URL`/`ADMIN_LOCKOUT_WORKER_SECRET` (the direct-to-Cloudflare `CLOUDFLARE_ACCOUNT_ID`/`CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_KV_NAMESPACE_ID` vars from the rejected KV attempt are removed, never having reached production). The `requireAdminAuth` check-then-act gap between `isLocked()` and `recordFailure()` (two concurrent requests that both read "not locked" can both proceed) is unchanged and explicitly out of scope — closing it would require serializing the token comparison itself, which the threat model doesn't justify; what this fix closes is the counter itself losing increments, not that outer race. **Evidence:** `server/lib/adminLockoutStore.test.js` rewritten again for the client-RPC shape (30 tests: local fallback, durable accumulate/lockout/reset, client HTTP/timeout/malformed-response fail-safes, IPv6 /64 normalization, and 8 CONCURRENCY-prefixed tests that route through the *real* `AdminLockoutCounter` class run in-process — 10 simultaneous failures on one key, same-IPv4 concurrent failures, same-IPv6-/64 concurrent failures across 4 distinct addresses, concurrently crossing the lockout threshold, repeated concurrent attempts while already locked, expiry after a concurrent burst, reset after a concurrent burst, and a negative control that reproduces the old KV race to prove it's real). New `server/lib/adminLockoutWorkerClient.test.js` (15 cases: request/auth-header construction, trailing-slash URL handling, non-2xx/malformed-body/network/timeout error handling, secret-never-in-error-message). New `worker/src/adminLockoutDurableObject.test.js` (14 cases, run both standalone and as part of the full worker suite: status/record/reset semantics, window expiry, unknown-action/malformed-body 400s, 5 concurrency tests firing genuinely concurrent unawaited calls at one shared in-process instance, and a negative control). `npm test` (server/): 270/270 pass. `npm test` (worker/): 43/43 pass (29 pre-existing + 14 new). `node --check` clean on `index.js`, `adminLockoutStore.js`, `adminLockoutWorkerClient.js`, `worker/src/index.js`, `worker/src/adminLockoutDurableObject.js`, and all four test files; `worker/src/index.js` also verified to import cleanly as ESM. |
| S05-L1 | **Fixed** (S3-14) | Verified | `/admin/api/account/lookup` accepted any non-empty uid <=128 chars — the one uid-taking admin route that skipped `validAdminUid()`, unlike the other three (`/locked/unfreeze`, `/duress/enroll`, `/duress/revoke`), letting a slash-bearing uid (`?uid=a/b/c`) reach `.doc()` as a multi-segment path. | **Fix (`server/index.js:4413`):** the route now calls `validAdminUid(uid)` instead of the old `!uid \|\| uid.length > 128` check, same guard the other three routes already used. **Also extended `validAdminUid()` itself (`server/lib/pure.js`):** it now rejects the Firestore-reserved single-segment ids `"."`, `".."`, and `/^__.*__$/` — none contain a slash/backslash/control char, so the pre-fix whitelist accepted them and `.doc()` throws on them (an uncaught 500 instead of an ordinary 400) on every route that already used this whitelist. **Evidence:** `grep -n "validAdminUid(uid)" index.js` — 4/4 uid-taking admin routes (`account/lookup:4413`, `locked/unfreeze:4257`, `duress/enroll:4455`, `duress/revoke:4512`) now call it. New tests: `pure.test.js` "validAdminUid rejects Firestore-reserved document-id shapes" (`.`, `..`, `__foo__`, plus non-matching `_user_123`/`user__123`/`...ellipsis` to prove no over-rejection); `s3_14_admin_remediation.test.js` "S05-L1: GET /admin/api/account/lookup validates uid with validAdminUid()". `npm test`: 227/228 pass (the 1 failure is `lib/identityVerify.test.js`, pre-existing/unrelated — missing native `@signalapp/libsignal-client` module, reproduced identically with this session's changes stashed out). `node --check index.js lib/pure.js` clean. |
| S05-L2 | **Fixed** (S3-14) | Verified | Every admin POST route called `collectBody()` first and ran `requireAdminAuth()` only inside the body's `"end"` callback — the exact inverse of `collectBody`'s own doc comment, letting an unauthenticated caller make the server buffer a body and hold a socket open (no `server.requestTimeout`/`headersTimeout` override existed, so Node's defaults — 300s request timeout on the Node 20 `package.json` pins — applied) before ever being rejected. | **Fix (`server/index.js`):** new `requireAdminAuthThenBody(req, res, onBody)` helper (`:1319`) runs `requireAdminAuth()` first and only calls `collectBody()` on success; on failure it `req.destroy()`s rather than leaving the request to drain. All 6 admin POST routes (`waitlist/approve`, `waitlist/deny`, `locked/unfreeze`, `sessions/revoke-all`, `duress/enroll`, `duress/revoke`) now call this wrapper instead of a bare `collectBody`. Also added explicit `server.headersTimeout = 15_000` / `server.requestTimeout = 30_000` (`:4591-4592`) so the remaining unauthenticated exposure (header-only requests, any body-reading route) is bounded rather than left at Node's 60s/300s defaults. Updated the now-accurate `collectBody` doc comment (`:1236`) to describe the fixed ordering instead of the aspirational-but-false claim it made before. **Evidence:** `s3_14_admin_remediation.test.js` — "S05-L2: requireAdminAuthThenBody runs auth before reading the body" (asserts `requireAdminAuth` call index < `collectBody` call index inside the helper, plus `req.destroy()` on failure), "S05-L2: every admin POST route is gated via requireAdminAuthThenBody, not a bare collectBody" (all 6 routes), "S05-L2: server.headersTimeout and server.requestTimeout are explicitly bounded". Updated the two pre-existing `adminAuditWiring.test.js` assertions that literally matched the old `requireAdminAuth(req, res)` inline-call text (deny, revoke-all) to assert `requireAdminAuthThenBody(req, res` instead — same intent (auth-gated), corrected to the new, more-correct wiring. `npm test`: 227/228 pass (same pre-existing unrelated failure as S05-L1). `node --check index.js` clean. |
| S05-L3 | **Fixed** (S3-14 — tracker was stale, not fixed) | Verified | Tracker previously claimed Fixed/Verified ("all admin API/login/logout responses now set `Cache-Control: no-store`"), but source did not support that claim: `setBaselineSecurityHeaders()` set seven security headers and never `Cache-Control`, and grepping every `/admin/api/*` `writeHead(200, { "Content-Type": "application/json" })` call site confirmed none set it inline either — only `GET /admin` (the login shell) and the login/logout redirects had it. Per SESSION_PROTOCOL §3 ("source beats tracker"), re-fixed rather than left closed. | **Fix (`server/index.js:2136`, `setBaselineSecurityHeaders`):** now sets `Cache-Control: no-store` and `Vary: Cookie` on every response whose `req.url` starts with `/admin`, applied via `res.setHeader()` before routing. Verified Node's `res.writeHead(status, headers)` OVERWRITES (does not merge under) a same-named header already set via `res.setHeader()` — confirmed empirically with a throwaway `http.createServer` probe — so `GET /admin`'s own stricter `Cache-Control: no-store, no-cache, must-revalidate` (plus `Pragma`/`Expires`) set later via `writeHead` still wins; this is additive to every JSON API route, not a regression on the one route that already had something stronger. `Vary: Cookie` added because authorization on every one of these routes depends on the session cookie. **Evidence:** `s3_14_admin_remediation.test.js` "S05-L3: setBaselineSecurityHeaders sets Cache-Control: no-store and Vary: Cookie for /admin*". `npm test`: 227/228 pass (same pre-existing unrelated failure). `node --check index.js` clean. |
| S05-L4 | **Fixed** (S3-14) | Verified | (1) TOCTOU: approve and unfreeze both did `get()` → branch → `update()`/`set()` outside any transaction — two concurrent approvals/unfreezes of the same doc both observed the pre-write state and both wrote, producing two audit entries for one effective action (corrupting the record S05-H3 made authoritative). (2) Unbounded reads: `GET /admin/api/locked` (`accountLock where locked==true`) and `GET /admin/api/duress/enrolled` (`duressEligibility where eligible==true`) had no `limit()`, unlike the waitlist query (`limit(200)`) and audit log (`limit(100)`) elsewhere in the same file — and clients can create their own `accountLock` doc per `firestore.rules`, so the set grows with the user base. | **Fix (`server/index.js`):** approve (`:4129`) and unfreeze (`:4302`) now wrap their read-check-write in `db.runTransaction(async (txn) => {...})`, reading via `txn.get(ref)` and writing via `txn.update()`/`txn.set()`; a losing/retried transaction re-reads the now-mutated doc and takes the already-handled (404/409) branch instead of writing or auditing again. Unfreeze's transaction additionally re-checks `snap.data().locked !== true` inside the transaction (not just doc existence), since the pre-fix code wrote unconditionally once the doc existed. `GET /admin/api/locked` (`:4235`) and `GET /admin/api/duress/enrolled` (`:4390`) both add `.limit(500)` — an accepted, documented degradation (no pagination added; the finding's fix asked for limit+pagination+total-count, this session scoped to the `limit()` half, which closes the unbounded-materialization risk). **Evidence:** `s3_14_admin_remediation.test.js` — "S05-L4: waitlist approve wraps its read-check-write in db.runTransaction", "S05-L4: locked/unfreeze wraps its read-check-write in db.runTransaction and re-checks locked===true", "S05-L4: GET /admin/api/locked and GET /admin/api/duress/enrolled both bound their query with limit()". `npm test`: 227/228 pass (same pre-existing unrelated failure). `node --check index.js` clean. **Not in scope:** pagination + total-count surfaced to the admin panel UI, left open for a future pass if the 500-doc cap is ever hit in practice. |
| S06-L1 | Fixed | Verified | Nonce expiry fails open on malformed `expiresAt` — **tracker was stale, not the code.** `/duress-lock`'s transaction (`index.js:3626` prior to this session) already validated the expiry shape explicitly and threw a `401` rather than a bare `TypeError`/fail-open comparison. This session extracted that check into a pure, directly-testable `pure.isNonceUsable(nonceUid, expiresAt, now)` (+ `pure.resolveNonceExpiry`) in `server/lib/pure.js`, wired it back into the transaction, and added 9 regression tests in `pure.test.js` covering: Firestore-Timestamp-shaped input, plain `Date` input, missing/malformed `expiresAt` (must resolve to `null`, never throw), missing uid, already-passed expiry, the exact-instant boundary, and the two literal old-bug regressions (bare `TypeError` on missing `expiresAt`; fail-open on an unparseable string). |
| S06-L2 | **Fixed** (pre-existing, S3-08) | Verified | `/duress-lock` unauthenticated, no rate limit | Re-verified from source this session, per SESSION_PROTOCOL §3 ("source beats tracker") — stale-`open` in the tracker but already remediated. Two parts: (1) unauthenticated by design (the caller has been wiped and signed out — see `index.js:3464-3468`'s comment on why a static APK-embedded secret is deliberately NOT used here), authorized instead by a single-use, uid-bound nonce issued while the user was still signed in (`/requestLockNonce`); (2) `checkIpRateLimit(clientIp)` (`index.js:3481-3487`) throttles the endpoint per-IP, 429ing and logging on rejection. Not reimplemented, not re-tested — no new code, tracker corrected in place. |
| S06-L3 | **Fixed** (S3-10, emulator-verified S3-15b) | Verified | `_duressNonces` no rules-test coverage — **tracker was stale, not the code.** `firestore-tests/rules.test.js` (`describe('/_duressNonces/{nonce}')`, `:1595-1649`) already carried 8 cases — exceeding the finding's minimum four (authed/anon × read/write denied): owner-of-bound-uid read denied, stranger read denied, anon read denied, create denied, re-bind-to-self update denied, expiry-extension update denied, delete(consume) denied, anon write denied. The paired `waitlist` collection the finding also named is likewise covered (`:1750-1782`, 6 cases). Re-confirmed this session via `grep`, not re-read from a stale index line number. **S3-15b (this session):** installed Java 21 + firebase-tools 15.26.0 and actually ran `firebase emulators:exec --only firestore` against the full `firestore-tests` suite — all 8 `_duressNonces` cases and all 6 `waitlist` cases passed against the live emulator, part of a 186/186 full-suite pass. The execution gap this row previously flagged is now closed. |
| S06-L4 | **Partial** (S3-19) | Verified | `AccountLockWorker` reports failure as success | Claim re-confirmed before fixing: `doWork()`'s HTTP-error and network-exception branches both returned an unconditional `Result.retry()` — WorkManager reschedules a retried `OneTimeWorkRequest` forever with no built-in attempt cap, so a permanently unreachable server or dead network retried silently forever with no genuine-failure signal ever surfacing anywhere. **Fix (`AccountLockWorker.java`):** added `MAX_ATTEMPTS = 15` and a `retryOrGiveUp(String reason)` helper that reads `getRunAttemptCount()` (the real WorkManager-tracked count, not a locally re-invented one) — below the cap it returns `Result.retry()` unchanged (transient-failure semantics preserved exactly), at/above the cap it logs via `Log.e` (kept in release builds per `proguard-rules.pro`) and returns `Result.failure()` instead of rescheduling forever. Both the generic-HTTP-error and network-exception branches in `doWork()` now route through this helper; the pre-existing terminal branches (`400`/`403` bad-or-consumed nonce, `401` expired nonce) are unaffected — they already returned `Result.success()`/were terminal and do not consume the retry budget. 15 attempts of `BackoffPolicy.EXPONENTIAL` starting at 30s accumulate to roughly 33 hours, comfortably longer than the class's documented 24-hour server-side nonce expiry, so a merely-slow network still gets its full window. The exhausted-retries branch deliberately does **not** clear `PendingLockStore`'s durable pending-lock intent — `hasLockIntent()` keeps reporting "believed unlocked" so the failure stays observable instead of silently disappearing, mirroring the same reasoning already used on the pre-existing `401` branch. New regression test `AccountLockWorkerBoundedRetryTest.java` (plain-JVM source-inspection, mirroring `ProguardRulesNarrowingTest`'s style — `Worker`/`WorkManager` runtime is not exercisable without Robolectric+Android SDK, unavailable here) asserts: a finite numeric `MAX_ATTEMPTS`; `retryOrGiveUp` consults `getRunAttemptCount()` and compares against `MAX_ATTEMPTS`; the exhaustion branch returns `Result.failure()` and not `Result.retry()`; the below-cap path still returns `Result.retry()`; both retryable `doWork()` branches route through `retryOrGiveUp()` (no bare `return Result.retry()` remains in `doWork()`); the `400`/`403`/`401` terminal codes are still present. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK in this environment — source-reviewed and brace/paren/bracket balance-checked, not compiled or run against a real/mocked `WorkManager`; AND-lane verification routed to **S3-19b**. |
| S07-L1 | **Partial** (S3-17) | Verified | `fetchGroupKey` creator check fails open | Claim re-confirmed before fixing: `GroupChatActivity`'s group-key-doc sender check read `creatorUid != null && !creatorUid.equals(sender)` — when the locally-cached `creatorUid` was `null` (legacy Room row from before `creatorUid` was populated, or a sync gap), the whole condition short-circuited to `false` and let **any** claimed `sender` through unchecked. **Fix:** changed to `creatorUid == null || !creatorUid.equals(sender)` — a missing/null `creatorUid` now fails **closed** (denies unconditionally), exactly like a mismatched one, since a missing `creatorUid` means there is no basis to trust the key doc's sender at all. Also redacts both `sender` and `creatorUid` via `LogRedact.uid()` in the accompanying `Log.w` (S07-L4/S10-N2, same session). **Gap:** no dedicated instrumentation/Activity-level test was added for this specific branch this session — verified by source read only, consistent with this codebase's general lack of Activity-level test infrastructure. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — source-reviewed and structurally checked, not compiled/tested; AND-lane verification routed to **S3-19b**. See `SESSION-S3-17.md`. |
| S07-L2 | **Partial** (S3-16) | Verified | Static `derivationCache` survives duress wipe | Claim re-confirmed before fixing: `SeedPhraseHelper.deriveIdentityKeyPair()` cached the derived `IdentityKeyPair` (identity private key) in a static `AtomicReference<CachedDerivation>` that `WipeHelper.eraseLocalData()` never touched — every on-disk copy (SecurePrefs, SQLCipher DB) was destroyed on wipe, but the in-memory cache outlived all three wipe paths (voluntary, unpair, duress) for the rest of the process's life. **Fix:** added `SeedPhraseHelper.clearDerivationCache()` (nulls the `AtomicReference`) and called it from `WipeHelper.eraseLocalData()` Step 4 alongside the `SecurePrefs` clear, for all `WipeMode` values. Best-effort by nature — this class has no way to zero the key bytes inside libsignal's object graph before GC, same limitation as every other in-memory key handle once it leaves this class. New regression test `clearDerivationCache_nullsOutTheBackingField` (`SeedPhraseHelperTest.java`, reflection-based, no libsignal native dependency) proves the backing field is actually nulled, not just that the method returns without throwing. Toolchain: no JDK/Gradle/Android SDK in this environment — source-reviewed and brace/paren-balance-checked, not compiled; lands `partial — AND verification BLOCKED` pending S3-19b per the Round-3 plan's exit criteria for this session. |
| S07-L3 | **Partial** (S3-16) | Verified | Mnemonic canonicalization/Locale | Claim re-confirmed before fixing: `SeedPhraseHelper.mnemonicToSeed()` only trimmed the two ends and NFKD-normalised — it did not lower-case or collapse internal whitespace itself, trusting every caller to have already canonicalised the string. `validateMnemonic()` DOES tolerate mixed case and repeated whitespace (`String.toLowerCase()` per word, split on `\s+`), so a mnemonic that validated successfully could silently derive a different seed/identity than its canonical form (e.g. a doubled clipboard-paste space, or a capitalised first letter) with no error raised anywhere — and only one of the three call sites (`RestoreFromSeedActivity`) pre-canonicalised at all. Also, both existing `.toLowerCase()` calls used the platform default locale rather than `Locale.ROOT` — Turkish-locale `'I'` → dotless `'ı'` is the canonical failure mode, so the same bytes could canonicalise differently by device language. **Fix (`SeedPhraseHelper.java`):** added `canonicalizeMnemonic()` (trim + `Locale.ROOT` lower-case + `\s+`→single-space collapse) and call it from inside `mnemonicToSeed()`, so the method is self-sufficient regardless of caller pre-processing; fixed `validateMnemonic()`'s per-word lower-case and `RestoreFromSeedActivity`'s pre-canonicalisation to use `Locale.ROOT` explicitly. Cryptographic semantics preserved exactly: NFKD normalisation, salt, and PBKDF2 parameters are unchanged — only the string handed to NFKD changed, and only for inputs that were already meant to be equivalent per `validateMnemonic()`'s own tolerance. New regression tests (`SeedPhraseHelperTest.java`, plain JUnit, no libsignal dependency): case-insensitivity, internal-whitespace-collapse, and surrounding-whitespace tests against a known BIP39 test vector, each asserting byte-identical seeds. Toolchain: no JDK/Gradle/Android SDK in this environment — source-reviewed and brace/paren-balance-checked, not compiled; lands `partial — AND verification BLOCKED` pending S3-19b per the Round-3 plan's exit criteria for this session. |
| S07-L4 | **Partial** (S3-17) | Verified | `loadSession` silent fresh-session substitution | Claim re-confirmed before fixing: `DuoShieldSignalStore.loadSession` caught any deserialization failure on a stored session row and silently returned a fresh, empty `SessionRecord` — but `containsSession()` only counts rows, so it kept reporting `true` for a row that was actually unusable; `establishSession`'s fast path believed a session already existed, skipped X3DH renegotiation, and every subsequent `encrypt` silently ran against an empty ratchet with nothing telling the user or peer why decryption started failing. **Fix:** on deserialization failure, the corrupt row is now deleted (so `containsSession()` correctly reports `false` afterward and a real X3DH renegotiation can happen) and a new typed `SessionDeserializationException` (`RuntimeException`) is thrown instead of returning a fresh session — callers must handle this explicitly rather than silently succeeding against the wrong ratchet state. Verified this does not introduce an uncaught-crash regression: `loadSession`'s only in-repo caller besides libsignal's internal `SessionStore` dispatch is `loadExistingSessions` (no local catch), but every app-level call site that invokes libsignal APIs backed by this store (`SignalCipherHelper`/`GroupCipherHelper` callers in `GroupChatActivity`, etc.) already wraps those calls in `catch (Exception ...)`, and `RuntimeException` **is** an `Exception`, so the new exception surfaces as a caught, logged failure rather than an uncaught crash. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — source-reviewed and structurally checked, not compiled/tested; AND-lane verification routed to **S3-19b**. See `SESSION-S3-17.md`. |
| S08-L1 | **Partial** (S3-19) | Verified | Deep link accepts unvalidated Account ID | Claim re-confirmed before fixing: `AddContactActivity.handleDeepLink()` populated the Account ID field directly from the deep-link URI segment with no shape check — `duoshield://add/<anything>` could feed arbitrary attacker-controlled input straight into a live `identities/{userId}` Firestore lookup (document IDs otherwise accept almost any non-empty string ≤1500 bytes) and then into visible UI/toast strings. **Fix:** new `contacts/AccountIdValidator.java` is the single source of truth for the canonical Account ID shape (three groups of unambiguous base32 chars — digits 2-9, A-Z excluding I/L/O, e.g. `K3MNP-Q8RXA-7BC`) via `isValid(String)`/`canonicalizeOrNull(String)` (trims + uppercases before matching) and an exposed compiled `pattern()`. `ContactManager`'s pre-existing public `ACCOUNT_ID_PATTERN` field/`canonicalizeAccountId()` method now delegate to it instead of maintaining a second, independently-editable copy of the regex. Every entry point — manual entry (`ContactManager.addContact()`, which now enforces the pattern at the point the ID is used, not merely in UI), clipboard auto-paste, and the deep link itself (`AddContactActivity.handleDeepLink()`) — validates through this shared pattern before the ID is ever populated or used for a lookup; an invalid deep-link Account ID is rejected rather than silently accepted or passed through. Format itself is unchanged from what the app already generates, so no valid existing Account ID is rejected. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — source-reviewed and brace/paren-balance-checked, not compiled/tested; AND-lane verification routed to **S3-19b**. |
| S08-L2 | **Partial** (S3-19) | Verified | Clipboard writes without `EXTRA_IS_SENSITIVE` | Claim re-confirmed before fixing: none of the app's `ClipData` writes (Account ID copies in `AddContactActivity`/`MessageAdapter`, UID copies in `ChatMediaActivity`) marked the clip sensitive, so on Android 13+ the system clipboard-access toast/preview could surface a plaintext Account ID/UID to anything nearby. **Fix:** `util/ClipboardHelper.java` gained a shared `markSensitive(ClipData)` helper that sets `ClipDescription.EXTRA_IS_SENSITIVE` on the clip's description `PersistableBundle`, guarded by `Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU` (API 33) so it is a safe no-op on the app's lower `minSdk` — `compileSdk 34` in `app/build.gradle` covers the API surface. Every direct sensitive clipboard write found by `grep`-ing all `setPrimaryClip`/`ClipData.newPlainText` call sites was updated to call it before `setPrimaryClip`: the Account ID copy in `AddContactActivity.copyMyId()` (missed in this session's first implementation pass, found and fixed on re-verification), the Account ID clipboard auto-paste path, and the UID copies in `ChatMediaActivity`/`MessageAdapter`. The one clipboard write intentionally left unmarked — `ChatMediaActivity`'s upload-error diagnostic text — is non-sensitive B2 error output, not account/identity material, and marking it would not match the finding's scope. New regression test `ClipboardHelperSensitivityTest.java` (plain JUnit, structural) covers the helper. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — source-reviewed and brace/paren-balance-checked, not compiled/tested (in particular, `EXTRA_IS_SENSITIVE` behavior is only observable on a real API 33+ device/emulator); AND-lane verification routed to **S3-19b**. |
| S08-L3 | **Partial** (S3-19) | Verified | PIN length stored beside PIN hash | Claim re-confirmed before fixing: `PinManager` persisted the real PIN length (`device_gate_pin_length`, and an account-scoped equivalent) in plaintext beside the salt:hash string — a passive read of `SharedPreferences`/backup data revealed exactly how many digits a user's PIN was, meaningfully narrowing brute-force search space without needing the hash at all. **Fix (`PinManager.java`):** stopped writing the length key entirely from `setDevicePin()`/`setPin()`; `getPinLength()`/`getDevicePinLength()` now always return the fixed `MAX_PIN_LEN` (6) upper bound instead of a per-account real value, and a legacy-migration path scrubs any pre-existing length key out of both the isolated device-gate store and the legacy account-scoped store on first read so old installs don't carry the plaintext value forward. **Regression found and fixed during re-verification this session:** removing the real length broke every numpad UI that auto-submitted exactly at the account's true (possibly `<6`-digit) PIN length — `LockScreenActivity` and `DevicePinGateActivity` both sized their digit buffer/auto-submit to `getPinLength()`/`getDevicePinLength()`, so a PIN shorter than the fixed 6-digit bound could never reach the auto-submit trigger and the user was locked out with no visible submit button. Fixed by adding a debounced auto-submit: once at least `PinManager.MIN_PIN_LEN` (4) digits are entered, a short pause (600ms) with no further digit press submits the buffer as-is; every digit press or backspace cancels the pending submit, so a still-typing user is never interrupted, while a user who stops at their real (shorter-than-max) length is still verified. `DuressManager.setDuressPin()`'s length gate had the same regression (exact-match against the now-fixed `getPinLength()`) and was changed to accept any length in `PinManager.MIN_PIN_LEN..MAX_PIN_LEN`, the same range `ManageUnlockCodesActivity` already enforces before calling it. New regression test `PinLengthNotPersistedTest.java` (plain JUnit against the existing `SecurePrefs` test-override seam, no Android runtime) covers: no length key written for a 4-digit or 6-digit device PIN; `getDevicePinLength()` always returns `MAX_PIN_LEN` regardless of the real PIN's length; and the legacy-migration path scrubs the length key from both the isolated and legacy stores. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — source-reviewed and brace/paren-balance-checked, not compiled/tested (the debounced-auto-submit UI flow in particular needs a real device/emulator run to confirm feel and timing); AND-lane verification routed to **S3-19b**. |
| S08-L4 | **Partial** (S3-18) | Verified | Lock screen over rendered activity, in recents | Claim re-confirmed before fixing: `AndroidManifest.xml`'s `.LockScreenActivity` entry had no `android:excludeFromRecents`, `android:noHistory`, or `android:launchMode` — the finding's "neither is excluded from recents" was literally true. The other half of the finding's own fix list — "set `FLAG_SECURE` on it rather than clearing it" — was **already closed** as a side effect of this same session's S08-H2 fix: `LockScreenActivity.onCreate()` now calls `BaseActivity.applyScreenshotSecurity(this)` (secure-by-default) instead of unconditionally clearing the flag, so the recents-*snapshot-content* risk the finding calls out ("the mechanism by which the recents snapshot ends up depicting the unlocked screen") is closed for any user who has not explicitly opted into the screenshots-allowed preference. **Fix (`AndroidManifest.xml`, commit `e284fb5`):** added `android:excludeFromRecents="true"` (keeps the lock screen, and the task's recents thumbnail while it is on top, out of the system recents list) and `android:launchMode="singleTask"` (backs the existing `BaseActivity.lockScreenActive` guard so a second `FLAG_ACTIVITY_NEW_TASK` start cannot stack a duplicate instance) to the `.LockScreenActivity` entry. **Why Partial, not Fixed:** the finding's remaining recommendation — decide the lock in `onCreate` before the underlying activity inflates, or show an opaque overlay immediately, rather than in `onStart` after content has already been laid out — was deliberately **not** attempted this session. `BaseActivity.onStart()` is the single choke point every screen routes lock/sign-out/duress-credential logic through; moving that decision earlier would mean either short-circuiting every subclass's `onCreate()` before it calls `setContentView()` (a call-order change touching every `BaseActivity` descendant) or adding an overlay view to every screen's layout, and neither can be regression-tested without a compiler in this environment — a wrong reordering here risks breaking normal navigation, which is a worse outcome than the residual, now-narrowed ordering gap (a brief moment where content is laid out before being covered, only exploitable via a recents capture, and only when the user has explicitly disabled the secure-by-default screenshot preference). No JDK/Gradle/Android SDK in this environment — source-reviewed and confirmed against the live XML with `xmllint --xpath` (the target attributes render exactly as written, only one `.LockScreenActivity` entry exists in the manifest), not compiled; lands `partial — AND verification BLOCKED` pending **S3-19b**. New regression coverage: `LockScreenManifestTest.java` (plain JVM `DocumentBuilderFactory` XML parse, no Android SDK/Robolectric needed) asserts `.LockScreenActivity`'s manifest entry carries both `android:excludeFromRecents="true"` and `android:launchMode="singleTask"` — the assertions were hand-verified against the real manifest via `xmllint --xpath` in this session (exact match) but the test itself has not been compiled/run, same toolchain blocker as everything else routed to S3-19b. |
| S10-N2 | **Partial** (S3-17) | Verified | Peer uid in release logcat | Claim re-confirmed before fixing: `DuoShieldSignalStore.java` and `GroupChatActivity.java`'s group-key-sender check interpolated raw peer uids (`address`, `address.getName()`, the session `key` string embedding the peer uid, `sender`/`creatorUid`) directly into `Log.w`/`Log.e` calls — lines that survive release builds per `proguard-rules.pro` (which deliberately keeps `Log.w`/`Log.e` for real failure diagnostics while stripping `Log.d`/`Log.i`), directly violating the product's metadata-resistance threat model. **Fix:** every such call site in the two touched files now routes the peer identifier through the pre-existing `LogRedact.uid()` helper (stable-per-process, salted SHA-256 tag — not a new utility, already used elsewhere in the codebase, e.g. `SignalSessionManager.java`) before logging: `DuoShieldSignalStore.saveIdentity`/`isTrustedIdentity`/`getIdentity`/`storeSession`'s failure logs, and `GroupChatActivity`'s sender-mismatch log. **Verification this session:** `grep`'d every `Log.w`/`Log.e` call across `app/src/main/java/com/duoshield/app` that references an `address`/`sender`/`creatorUid`/`uid`-shaped variable — all now go through `LogRedact.uid()` except lines that log a constant string with no actual uid value (e.g. `"register: uid still null after ... retries"`), which are not an instance of this finding. No raw-peer-uid `Log.w`/`Log.e` line remains in the files this session touched. **Why Partial, not Fixed:** this session's fix is scoped to the two files it touched for S07-H2/H3/M2/M3/L1/L4 (`DuoShieldSignalStore.java`, `GroupChatActivity.java`); a full-repo sweep for any other release-surviving raw-uid log line outside those files was not performed and is not claimed. Also, no JDK/Gradle/Android SDK here — source-reviewed and structurally checked, not compiled/tested; AND-lane verification routed to **S3-19b**. See `SESSION-S3-17.md`. |
| S10-N3 | **Partial** | Verified | Deleted media survives B2 cold tier on race — a race guard now exists (`worker/src/index.js:553-574`, "Race guard: the nightly migration PUTs to B2 and THEN deletes from R2"); confirm end-to-end before closing | Re-verified this session (S3-12, no code changes to this guard): `worker/src/index.test.js` **29/29 pass**, including the 2 migration-race regression tests added in the prior session covering the compensating-delete path (an R2 re-HEAD after the B2 PUT comes back null — object was deleted mid-migration — triggers a best-effort delete of the just-written B2 copy so a deleted object can't resurrect from cold storage). `node --check worker/src/index.js` clean. Re-read the guard end-to-end this session and confirmed it is correctly scoped: it only fires on the `current === null` branch and does not touch the two other pre-existing, unrelated race guards in the same function. **Still Partial, not Fixed:** production runtime verification against a real Cloudflare Worker + R2 + B2 deployment remains **BLOCKED** in this environment (no `wrangler`/live Cloudflare account access here) — code and unit-test evidence is as strong as this environment can produce, but the actual migration job has never been observed to run end-to-end against live R2/B2. |
| SC-11 | **Accepted** (S3-20) | Verified | Production crypto depends on an alpha library | Re-verified this session: `app/build.gradle:213` still declares `implementation 'androidx.security:security-crypto:1.1.0-alpha06'`, unchanged. **Rationale (per `audit/SESSION-09-SUPPLY-CHAIN-CI.md`):** the audit itself rates this Low, not because the dependency is safe in the abstract, but because `alpha06` is widely deployed industry-wide with no known exploitable defect, and the "stable" `1.0.0` alternative the library offers is itself more limited — swapping to it would be a downgrade, not a fix. This is recorded as a maintenance-risk finding, not an active vulnerability, and the audit's own recommendation is to track it, not block on it. **Residual risk:** `EncryptedSharedPreferences` (which backs PIN material and key metadata reviewed in S06/S07/S08) receives no formal API or behavioral stability guarantee and no backported security fixes from an alpha channel; combined with `SC-03` (dependency hashes), this version is not yet hash-pinned either. **Operational dependency:** none blocking; the audit's stated follow-up is to hash-pin the current version via `SC-03` and, longer-term, consider consolidating small-secret storage onto the app's own SQLCipher-backed database + AndroidKeyStore path instead of `security-crypto` at all — a product/architecture decision out of scope for this reconciliation pass. |
| SC-12 | Open (operator) | Verified | Branch protection — **still unprotected.** Re-checked live this session (S3-01): `gh api repos/kumarclaude4-hash/DuoFatass/branches/main/protection` → `404 Branch not protected`. Not closable from source; operator must enable branch protection on `main`. Runbook item. |

## Informational (23)

| ID | Status | Confidence | Issue |
|---|---|---|---|
| S01-I1 | **Accepted** (S3-20) | Verified | Global read oracle on `users`/`identities` | Re-verified this session: `firestore.rules:40-41` (`match /users/{uid} { allow read: if request.auth != null; }`) and `firestore.rules:469-470` (`match /identities/{userId} { allow read: if request.auth != null; }`) — any authenticated account can still read any other account's `users`/`identities` doc; this is unchanged from the audit. **Rationale (per `audit/SESSION-01-FIRESTORE.md`):** the audit frames this as intentional (ECDH/X3DH key fetch, contact lookup, FCM token delivery need every account's identity doc to be reachable) but conditions acceptance on two things: (a) the docs are minimized to non-sensitive fields, and (b) the UID space is large enough that enumeration is infeasible. **Condition (a) is source-confirmed closed:** `S01-M2` (`firestore.rules` update on `identities/{userId}` now requires `keys().hasOnly(['uid','identityPubKeyHash'])`) and `S01-L2` (`users/{uid}` create/update now allow-lists `displayName`/`fcmToken`/`platform`/`photoUrl`/`updatedAt` with size/type bounds) are both now **Fixed** (`S3-15b` ran the emulator suite this session, 186/186 pass) — the field-minimization the audit demanded is in the rule text and emulator-verified. **Condition (b) is unchanged and not re-assessed here:** the UID format is still the deterministic `XXXXX-XXXXX-XXX` derived from `SHA-256(seed)` (see `S07-I3` below); no widening of the enumeration surface was found. **Residual risk:** enumeration + metadata-oracle capability remains — an authenticated attacker can still confirm which UIDs exist and harvest FCM tokens/display metadata, and (per the audit) can pair this with prekey-related findings to target a specific user; the field-minimization narrows *what* is harvested, not *that* enumeration is possible. **Operational dependency:** the audit's own instruction to "record the decision explicitly, do not leave it implicit" is what this row now does — no further code change is implied by this disposition, and `S3-15b` (not this session) is what promotes the minimization half from Partial to Fixed. |
| S01-I2 | **Accepted** (S3-20) | Verified | Pervasive `get()`-based cross-document authorization is a systemic TOCTOU + cost pattern | Re-verified this session: `grep -c "get(/databases" firestore.rules` = 20 — message, group-key, and call rules still authorize by `get()`-ing a parent doc (chat participants, group members/creator, call caller/callee), unchanged from the audit. **Rationale (per `audit/SESSION-01-FIRESTORE.md`):** the audit itself frames this as systemic rather than a single discrete defect — every such `get()` is a billed read (cost multiplies under automated abuse) and authorization reflects the parent doc *at rule-evaluation time*, so a concurrent membership change is an inherent race, "not fixable from within a single rule evaluation." The audit's own instruction is to acknowledge this in the model, not chase it as a line-item fix. **Confirmed still accurate:** `S01-M1`'s row (Fixed, `S3-15b`) makes the identical point explicitly in-rule — "the TOCTOU half... is an inherent property of every `get()`-based membership check in this ruleset... accepted/documented in-rule per the audit's own recommendation, not fixed" — so this session's acceptance is consistent with, not a new decision independent of, that already-landed rule comment. **Residual risk:** a membership/participant change (e.g., group-member removal) racing a `get()`-based authorization check can leave a narrow window where a rule evaluates against stale parent-doc state; this is bounded by each individual rule's own scope (e.g. `S01-M4`'s delete re-check narrows one instance) but not eliminated as a class. **Operational dependency:** none — this is a documented architectural property of Firestore security rules, not something a future session is expected to close from source. |
| S02-I1 | **Accepted** (S3-20) | Verified | Cold-contact + registration oracle in `createChat` (by design; cross-ref `S01-I1`) | Re-verified this session: `server/index.js:2901` still returns the literal string `"Partner not found"` on a 404 for a non-existent `userId`, distinguishable from the success path — `createChat` still lets any registered user create a chat doc pairing themselves with any other registered user with no consent step. **Rationale (per `audit/SESSION-02-SERVER-AUTH.md`):** the audit is explicit this "adds no capability beyond the already world-readable `identities` oracle (`S01-I1`)" — it is the server-side face of the same trade-off, not an independent hole, and the audit frames the decision as a single joint one with `S01-I1`: "accept as inherent to a messenger, or gate first contact behind a mutual/invite handshake." Accepting `S01-I1` (above, this session) without also accepting `S02-I1` would be inconsistent, since closing this endpoint alone would not remove the identical capability the `identities` collection already exposes. **Residual risk:** unsolicited first contact / spam is possible (any registered user can chat-request any other), and the distinguishable 404 confirms live-account existence for a given ID — unchanged from the audit's characterization. **Operational dependency:** none from this session; the audit's alternative (gate first contact behind a mutual/invite handshake) is a product-feature decision, not a code defect, and is not scheduled in `ROUND3_REMEDIATION_PLAN.md`. |
| S02-I2 | Open | Carried | In-memory limiters best-effort |
| S02-I3 | **Open — flagged, NOT accepted** (S3-20) | Verified | No `checkRevoked` on `verifyIdToken` | Re-verified this session: `grep -n "verifyIdToken" server/index.js` shows 8 live call sites (`:2640, :2862, :2988, :3070, :3386, :3540, :3647, :3710`), none passing `true` as the second (`checkRevoked`) argument — the gap the audit describes is unchanged and confirmed present. **`ROUND3_REMEDIATION_PLAN.md`'s S3-20 scope line lists this as `S02-I3 (checkRevoked half)`, implying a prior decision had already split this finding and accepted the `checkRevoked` portion.** This session searched `decisions/DECISION-LOG.md` and every prior session file for that split decision and **found no record of it ever being made or ratified** — the plan's premise does not match anything actually written down. **What IS source-confirmed as a genuine, relevant mitigating layer** (not previously credited on this row): `S06-H1` is now **Fixed** (`accountLock` checked inside the `/mintToken` transaction, `server/index.js:2053-2062`, blocking a locked account from minting a *new* token), and `server/index.js:3916` calls `admin.auth().revokeRefreshTokens(uid)` on lock (blocking refresh of an *existing* token), and `firestore.rules:139-141`'s `accountNotLocked()` independently gates the backup subsystem (the audit's own highest-value target for this class of gap, `S06-C2`) regardless of ID-token validity. **What remains genuinely open and is NOT being accepted here:** none of those three layers close the literal `checkRevoked` gap on non-mint, non-Firestore-rules-gated server routes (e.g. `/linkPreview`, `/turnCredentials`, `/mediaToken`) — an ID token issued before a lock/session-revocation event continues to authenticate those routes for up to its own ~1h natural lifetime, exactly as the audit describes, with `revokeRefreshTokens` only preventing a *new* token from being minted, not invalidating the one already in the caller's hand. **Disposition:** this row stays **Open**, not `Accepted`, because (1) no ratified accepted-decision exists to carry forward for the "checkRevoked half," (2) the residual window is real and un-mitigated on the specific routes named above, and (3) fabricating a split/accepted disposition here would misstate confidence per this session's own instruction not to invent acceptance the evidence doesn't support. **Recommended path if this is to become `Accepted` in a future session:** an explicit product decision (mirroring `S01-I1`'s "ratify explicitly" pattern) on whether the ≤1h residual window after lock/revocation is tolerable given the three mitigating layers above, OR a code fix threading `checkRevoked: true` through the highest-value subset of these 8 call sites (the ones reachable by a just-locked account) and accepting the added `admin.auth()` verification-service round-trip cost only there. |
| S03-I1 | **Accepted** (S3-20) | Verified | The Worker holds bucket-wide B2 credentials (TB-8) | Re-verified this session: `worker/src/index.js:32-39`'s `getB2Client(env)` still constructs a client from `B2_ACCESS_KEY_ID`/`B2_SECRET_ACCESS_KEY` with no object- or prefix-scoping, and the same Worker process also holds the R2 binding and `MEDIA_TOKEN_SECRET` — unchanged from the audit. **Rationale (per `audit/SESSION-03-MEDIA.md`):** the audit records this as "worth an explicit accepted-risk note," conditioned on checking whether Backblaze application keys can be scoped down — it is not framed as something this codebase alone can fix, since B2 key-scoping (if available on the account's plan) is configured at Backblaze, not in application code. **Residual risk (unchanged, not newly mitigated this session):** a Worker compromise, or a stolen Cloudflare API token (`SC`-series, Session 09), is total — read, overwrite, and delete of every user's media in both tiers (hot R2 + cold B2), plus the ability to mint the Worker's own capability tokens via `MEDIA_TOKEN_SECRET`. No blast-radius reduction (e.g. per-user or per-object B2 key scoping) has been implemented in this codebase, and none is claimed. **Operational dependency:** closing or narrowing this requires an operator action at Backblaze (checking whether application-key scoping to the bucket/prefix is available on the current plan and applying it) — not a source change, and not something S3-20 can execute from this environment. |
| S03-I2 | Fixed | Verified | Worker accounting no longer purely advisory — now backed by `perUserCounts` enforcement (see S03-H2) |
| S03-I3 | **Accepted** (S3-20) | Verified | `/mediaToken` is a scope-membership oracle, and denials are logged per attempt | Re-verified this session: `server/index.js`'s `callerMayAccessScope(uid, scopeId)` (`:665-679`) still `.get()`s both `chats/{scopeId}` and `groups/{scopeId}` and returns a distinguishable allowed/denied result at the `/mediaToken` route (`:3028-3029`), letting a caller test membership in arbitrary chat/group IDs. **Rationale (per `audit/SESSION-03-MEDIA.md`):** the audit is explicit this "adds little beyond the enumeration trade-off already recorded in `S01-I1`/`S02-I1`" (both accepted, above, this session) — accepting those two without also accepting this one would be inconsistent, since this is the same class of oracle at a third endpoint, not an independent escalation. The audit also notes `S03-H1` "makes it moot for any scope the attacker can shadow" — i.e. a separate, already-tracked finding narrows the practical value of this oracle further. **Residual risk:** an attacker can still probe arbitrary `chats`/`groups` IDs at up to the endpoint's rate limit (120/min per the audit) to learn which scope IDs exist/are accessible to a given account; the denial log line records `uidTag` + raw `scopeId`, which the audit flags as "consistent with the pseudonymisation policy, though `scopeId` is itself a conversation identifier" — i.e. the log entry itself does not leak identity beyond what the oracle already reveals live. **Operational dependency:** none — this shares the same accept-or-gate product decision as `S01-I1`/`S02-I1`; no separate action is implied. |
| S04-I1 | Open | Carried | `/status` and `/` unauthenticated, publish counters |
| S04-I2 | **Fixed** (S3-12) | Verified | Dead B2 presign surface; B2 creds still expected in env (= S03-L3) | Claim re-confirmed before fixing: a prior session (commit `e366730`) had already removed `b2PresignUrl`/`b2PresignUrlForUid` from `server/index.js` and their helpers/exports from `server/lib/pure.js`, but left obsolete tests and a stale env-var doc row behind. This session closed the remaining gap. **Fix:** deleted the 8 now-orphaned tests in `server/lib/pure.test.js` that exercised `buildB2PresignUrl`/`b2HmacKey` (exports that no longer exist — these would throw, not just fail, if run against current `pure.js`); removed the stale `B2_KEY_ID`/`B2_APPLICATION_KEY`/`B2_BUCKET`/`B2_REGION` row from `server/README.md`'s env-var table, since `server/index.js` no longer reads any of them; fixed a stray two-space indentation artifact left over from the `e366730` refactor (no logic change). **Repo-wide grep confirms no live references remain** to `b2PresignUrl`, `b2PresignUrlForUid`, `buildB2PresignUrl`, or `b2HmacKey` outside of comments documenting the removal and historical audit docs. The **operator action is still outstanding and cannot be closed from source**: the Backblaze B2 application key that `B2_KEY_ID`/`B2_APPLICATION_KEY` referred to (leaked into released APKs per S03-L1) must be revoked at Backblaze — nothing in this codebase can do that. The Cloudflare Worker's own B2 credentials (`B2_ACCESS_KEY_ID`/`B2_SECRET_ACCESS_KEY`/`B2_ENDPOINT`/`B2_BUCKET`/`B2_REGION`) are a separate, still-required credential set for the tiering job (S10-N3) and were intentionally not touched. **Evidence:** `server/lib/pure.test.js` 60/60 pass; `worker/src/index.test.js` 29/29 pass (unchanged, confirms no regression from this cleanup); `node --check server/index.js` and `server/lib/pure.js` clean. See `security-remediation/sessions/SESSION-S3-12.md`. |
| S04-I3 | Partial (S3-08) | Verified | Preview provenance/failure indistinguishable to client | Two independent parts. **Provenance (fixed):** `preview.domain` was derived from the originally-submitted URL while `title`/`imageUrl` were scraped from the final hop of a (per-hop validated) redirect chain — a link on `trusted.example` that 302s to `attacker.example` rendered a card labelled `trusted.example` carrying `attacker.example`'s content, a small phishing primitive. **Fix:** new pure `previewDomainFromUrl(finalUrl, fallbackHostname)` in `server/lib/pure.js` derives `domain` from `finalUrl` (already validated per-hop by `fetchFollowingSafeRedirects`, so this never surfaces an unvalidated host); `server/index.js`'s `/linkPreview` success path now calls it instead of using `parsed.hostname` — the failure/catch path is intentionally unchanged, since no content was actually fetched from anywhere on that path and the originally-submitted host remains the only defensible label. **Failure-indistinguishability (deferred, not fixed):** a blocked host, timeout, non-HTML response, and metadata-free page still all return the same bare `{url, domain}` with a `200` — that half of the finding needs a client-visible error/status field and Android-side handling, which is a coordinated client change, not a server-only fix; left open for a future session. **Evidence:** `lib/pure.test.js` (+3 `previewDomainFromUrl` cases: labels with the final hop not the original host — the exact phishing scenario, strips leading `www.`, falls back to `fallbackHostname` on an unparseable `finalUrl`) — `npm test` 201/201 pass; `node --check index.js`/`pure.js` clean. |
| S05-I1 | Fixed | Verified | Operator secrets undocumented — `server/README.md` now tables all env vars `index.js` reads |
| S05-I2 | **Fixed** (S3-14) | Verified | Two admin-surface comments described behavior the code no longer had, so a reviewer reasoning from the comments would draw the wrong security conclusion. (1) `collectBody()`'s doc comment claimed callers ran `requireAdminAuth()` before reading the body — the exact inverse of what every admin POST route actually did (auth ran inside the `"end"` callback). (2) The `ADMIN_PAGE_HTML` header comment described the page prompting for, holding in memory, and replaying the operator token as `x-admin-token` on every fetch — but the page had since moved to an HttpOnly session-cookie exchange (POST `/admin/login`) and no longer holds the token at all. | **Fix (`server/index.js`):** both comments were rewritten to match current source as part of S3-14's `collectBody`-ordering (S05-L2) and session-cookie (S05-M3/S05-I3) work — the `requireAdminAuthThenBody` doc block (`:1325`) now describes auth-before-body accurately, and the `ADMIN_PAGE_HTML` comment (`:1351`) now describes the session-cookie exchange with `x-admin-token` documented as the secondary scripted-caller path only. This session additionally caught and corrected a **third** stale comment that survived the earlier pass and directly undercut the S05-I3 rationale: the `requireAdminAuth` CSRF note (`:955`) asserted `SameSite=Strict` "already blocks the cross-site case," but the cookie is issued `SameSite=Lax` (`adminSessionCookie():901`, deliberately, because some Android in-app webviews drop a Strict cookie on the post-login top-level navigation). The comment now states `SameSite=Lax` and explains that Lax still withholds the cookie on cross-site sub-requests (the mutating-call case), preserving the accurate defense-in-depth story for the new Sec-Fetch-Site check. **Evidence:** `s3_14_admin_remediation.test.js` "S05-I3/S05-I2: the session cookie is SameSite=Lax and the CSRF comment does not misstate it as Strict" (asserts `adminSessionCookie` issues `SameSite=Lax` AND `requireAdminAuth`'s body contains no `SameSite=Strict` claim); the S05-L2 tests transitively guard the `collectBody`/`requireAdminAuthThenBody` comment by asserting the real ordering it now describes. `grep -n "SameSite=Strict" index.js` — zero hits. `npm test`: 229/230 pass (the 1 failure is `lib/identityVerify.test.js`, pre-existing/unrelated — missing native `@signalapp/libsignal-client` module). `node --check index.js` clean. |
| S05-I3 | **Fixed** (S3-14) | Verified | Three sub-items on the admin surface's most privileged credential paths. (1) **Cookie Secure flag decided by a client-suppliable header:** `adminSessionCookie()` set the `Secure` attribute from `forwardedProto === "https"` unconditionally — `x-forwarded-proto` is client-suppliable, so any caller could decide whether their own session cookie got `Secure` (never exploitable against a third party, but the wrong trust boundary on the system's most privileged cookie). (2) **`safeTokenEqual` length oracle:** flagged for an early `return false` on a raw length mismatch, leaking the token length by timing. (3) **CSRF defense-in-depth:** the mutating admin routes relied on the `SameSite=Lax` cookie (+ CSP `form-action 'self'`) as their only anti-CSRF mechanism, with no `Origin`/`Sec-Fetch-Site`/token cross-check. | **Fix (`server/index.js`):** (1) `adminSessionCookie()` (`:881`) now derives `isHttps` from `FORCE_SECURE_COOKIES` (explicit operator override, `:874`) else `(TRUSTED_PROXY_HOPS > 0 && forwardedProto === "https") || req.socket.encrypted` — the `x-forwarded-proto` header is consulted **only** when the operator has already declared a trusted proxy present via the existing `TRUSTED_PROXY_HOPS` (S04-M3), never on an unconditional client-suppliable value. (2) `safeTokenEqual` (`lib/pure.js`, exported at `index.js:865`) verified from current source to have **no** length-dependent short-circuit — it compares over a fixed span and folds any length difference into the mismatch accumulator, so equal-length-but-different and different-length inputs are indistinguishable by timing (this half was already landed and unit-tested; re-confirmed, not re-touched). (3) `requireAdminAuth()` (`:967-973`) now reads the browser-set `Sec-Fetch-Site` Fetch-Metadata header and rejects the unambiguous `"cross-site"` value with a `403` + `auditAdminEvent("admin_api_blocked_cross_site")`, **before** the IP-lockout and token-comparison paths — leaving `same-origin`/`same-site`/`none`/absent untouched so it is purely additive and cannot break a legitimate same-origin request or an older browser. `Sec-Fetch-Site` is set by the browser itself and cannot be forged by an attacker page, so it is a sound second CSRF signal layered on the `SameSite=Lax` cookie. **Evidence:** `s3_14_admin_remediation.test.js` — "S05-I3: adminSessionCookie only trusts x-forwarded-proto when a trusted proxy hop is configured" (asserts the `TRUSTED_PROXY_HOPS > 0 && forwardedProto === "https"` gate + the `FORCE_SECURE_COOKIES` override), "S05-I3: requireAdminAuth rejects Sec-Fetch-Site: cross-site with a 403 before any auth work" (asserts the exact-`"cross-site"` match, `403`, audit event, AND that the reject precedes `safeTokenEqual`), plus the SameSite=Lax comment-accuracy test shared with S05-I2. `pure.test.js` — "safeTokenEqual has no length-dependent short-circuit (S05-I3)" + equal/unequal-length cases across the 63/64/65-byte boundary. `npm test`: 229/230 pass (same pre-existing unrelated `identityVerify.test.js` failure). `node --check index.js lib/pure.js` clean. |
| S06-I1 | Fixed | Verified | Rules comment contradicting shipped admin unfreeze — `firestore.rules:421-430` now documents the real behavior explicitly |
| S06-I2 | Fixed | Carried | Step 1a can't tell success from failure |
| S06-I3 | **Accepted** (S3-20) | Verified | PIN strength is bounded by the 4–6 digit PIN space regardless of hashing cost | Re-verified this session: `app/src/main/java/com/duoshield/app/util/PinManager.java:39-56` still defines `MIN_PIN_LEN = 4`, a `MAX_PIN_LEN` upper bound of 6 (shared by app and device PIN), `ITERATIONS = 310_000`, `KEY_LEN = 256`, `PBKDF2WithHmacSHA256` — an offline attacker with the salt:hash pair still faces at most 10^6 candidates no matter the iteration count. **Rationale (per `audit/SESSION-06-DURESS.md`):** the audit's own framing is that 310,000-iteration PBKDF2-HMAC-SHA256 is reasonable for the *online*, rate-limited attempt path this app actually defends against (lock-out/wipe logic elsewhere in this session), and that no realistic iteration count fixes a 4-6 digit numeric space against an *offline* attacker who has already exfiltrated the salt:hash — that gap is inherent to choosing PIN-length UX over a passphrase, not a defect in the derivation itself. **Confirmed still consistent with this session's other findings:** `S08-L3` (landed this round, cited in the `PinManager` source comment) already closed the length-oracle half of this same problem — the stored record no longer reveals whether a given account's PIN is 4, 5, or 6 digits, so an offline attacker must brute the full unified range rather than a known-length slice. **Residual risk:** unchanged — offline brute force of an exfiltrated salt:hash pair is bounded by keyspace, not iteration count; explicitly accepted as a UX-vs-strength trade-off, not remediated. **Operational dependency:** none; the audit frames the alternative (raising `MIN_PIN_LEN`, or moving to alphanumeric) as a product/UX decision, not a code defect this session can close. |
| S07-I1 | **Accepted** (S3-20) | Verified | `SenderKeyStore` is an intentional no-op stub — group messaging never uses the Sender Key protocol | Re-verified this session: `DuoShieldSignalStore.java:496-505` — `storeSenderKey()` has an empty body, and `loadSenderKey()` unconditionally `return new SenderKeyRecord(new byte[0])` regardless of the `sender`/`distributionId` arguments; nothing is actually persisted or retrievable. **Rationale (per `audit/SESSION-07-CLIENT-CRYPTO.md`):** the audit's own finding is conditional on confirming group messaging never invokes the Sender Key path — this session re-confirmed `CreateGroupActivity.java` only exposes group *creation*, not a Sender Key distribution flow, and cross-references `S07-I2` (below): the app has no add-member flow at all, so there is no code path that would ever call `storeSenderKey`/`loadSenderKey` for a real distribution ID. Group messages are still delivered via pairwise Signal Protocol sessions (fan-out), not multicast Sender Key encryption — the stub is therefore dead code for the app's actual group-messaging design, not a silently-broken feature. **Residual risk:** none *if* group messaging never adopts Sender Key in the future without also implementing this store — this is a forward-looking trap for a future feature change, not a live gap in the current message flow. **Operational dependency:** none; this is accepted as intentional dead code, contingent on the same architectural fact `S07-I2` documents (no add-member/multicast group flow exists). |
| S07-I2 | **Accepted** (S3-20) | Verified | No add-member-to-existing-group flow exists at all — membership is fixed at creation | Re-verified this session: `CreateGroupActivity.java` still exposes only `createGroup(name, members)` (`:144`) — repo-wide search for `addMember`/`AddMember` across `app/src/main/java` returns no matches. There is no UI, activity, or server route that adds a member to an *already-created* group; the entire membership list is fixed at `createGroup` time. **Rationale (per `audit/SESSION-07-CLIENT-CRYPTO.md`):** the audit's finding title ("key-less add") describes a *hypothetical* risk that would arise if an add-member feature were built without re-keying/re-distributing session state to the new member — but since no such feature exists in this codebase, there is nothing to be "key-less" about yet. The audit accepts this as a documentation/future-guardrail item: whenever add-member ships, it must re-derive or redistribute group key material to the new member and must not let them silently see history they weren't a session-party to. **Residual risk:** none today — the gap is purely prospective. If an add-member feature is ever added without accompanying re-key logic, a new member could either fail to decrypt existing traffic or (worse, depending on implementation) be handed keys granting retroactive access to pre-membership history. **Operational dependency:** none for this session; this is a design constraint to honor in a future feature session, not a defect in current source. |
| S07-I3 | **Accepted** (S3-20) | Verified | Account ID is a deterministic, dual-purpose derivation of the seed with only 64 bits of entropy | Re-verified this session: `SeedPhraseHelper.java:603,616` — `deriveUserId(byte[] seed)` computes `SHA-256(seed)`, takes the first 8 bytes (64 bits), and Base32-encodes them into the `"XXXXX-XXXXX-XXX"` display format; called identically from `DisplayNameActivity.java:103`, `RestoreFromSeedActivity.java:204`, and `SeedPhraseDisplayActivity.java:242` — the same seed always yields the same account ID, and the ID doubles as both a public identifier and a value derived from (thus entangled with) the user's secret seed. **Rationale (per `audit/SESSION-07-CLIENT-CRYPTO.md`):** the audit accepts 64 bits as adequate collision resistance for the account's expected user population (birthday-bound collision risk is negligible at any realistic scale), and frames the dual-purpose derivation as a deliberate design choice (deterministic recovery — the same seed always reproduces the same account ID on restore) rather than an oversight; it does **not** leak the seed itself (SHA-256 is one-way) and does not shrink the seed's own entropy. **Residual risk:** the ID is globally enumerable/predictable *given* a seed, and — combined with `S01-I1`'s global read oracle — an attacker who obtains or guesses a seed can compute the exact account ID to target without an extra lookup step; this is the same enumeration surface already accepted under `S01-I1`/`S02-I1`/`S03-I3`, not a new independent exposure. **Operational dependency:** none; changing the derivation (e.g. widening to 128 bits, or decoupling ID from seed) is a breaking migration for every existing account and is not scheduled in `ROUND3_REMEDIATION_PLAN.md`. |
| S08-I1 | **Partial** (S3-19) | Verified | R8 keeps crypto member names | Claim re-confirmed before fixing: `proguard-rules.pro` blanket-kept `com.duoshield.app.crypto.**` and `com.duoshield.app.security.**` (full `-keep`/`-keepclassmembers` with `{ *; }`), disabling shrinking, obfuscation, and dead-code removal for exactly the classes — key management, PIN/lock, E2E crypto, duress handling — a static-analysis attacker on the APK most wants left readable. **Audit performed:** grepped both packages for `Class.forName`, `getDeclaredMethod`/`getDeclaredField`, `getMethod`/`getField`, `newInstance`, `@SerializedName`, and any `Gson` usage — none found; no reflection, JNI, or Gson field-mapping entry point in either package requires name/member preservation. **Fix:** removed the blanket `-keep`/`-keepclassmembers` rules for both packages outright (not narrowed to a subset — none of it was justified). The two app `Worker` subclasses that live in those packages, `AccountLockWorker` (`security`) and `SignedPreKeyRotationWorker` (`crypto.signal`), remain covered by the pre-existing generic `-keep class * extends androidx.work.Worker` and the `ListenableWorker` `(Context, WorkerParameters)` constructor keep rule further down the file — WorkManager's reflective instantiation of both classes is unaffected by removing the package-specific rules. `com.duoshield.app.models.**` (Room/Firestore reflective field mapping) was left untouched — the narrowing was scoped to crypto/security only. New regression test `ProguardRulesNarrowingTest.java` (plain-JVM regex/text read of the live `proguard-rules.pro`, no R8/Gradle invocation) asserts: no active (non-comment) blanket-keep line remains for either package; the generic `Worker`/`ListenableWorker` constructor keep rule is still present (so the two app Workers stay covered); and `com.duoshield.app.models.**`'s full keep is untouched. **Why Partial, not Fixed:** no JDK/Gradle/Android SDK here — the removal was verified by reading the file and grepping app source for reflection usage, not by running an actual R8/minify build to confirm nothing breaks under obfuscation; AND-lane (specifically an actual `assembleRelease`/`minifyReleaseWithR8` run) verification routed to **S3-19b**. |
| S08-I2 | **Accepted** (S3-20) | Verified | No TLS certificate pinning — the app trusts the platform CA store only | Re-verified this session: `app/src/main/res/xml/network_security_config.xml` declares only `<base-config cleartextTrafficPermitted="false"/>` — no `<pin-set>` block, no `<domain-config>` with `<pin-set>` entries, and no `CertificatePinner` usage anywhere under `app/src/main/java` (repo-wide grep returns nothing). Cleartext HTTP is blocked globally, but any certificate the device's trust store accepts (including a user- or MITM-installed CA) is accepted for the Worker/API endpoints. **Rationale (per `audit/SESSION-08-CLIENT-PLATFORM.md`):** the audit accepts this because certificate pinning has an operational cost this codebase has not built infrastructure for — pin rotation on cert renewal, and a "brick the app until an update ships" failure mode if a pin set goes stale — and the app's actual secret material (message content, keys) is already end-to-end encrypted independent of the TLS channel, so a MITM with a trusted-but-illegitimate cert gains network metadata and ciphertext, not plaintext. **Residual risk:** a network-level attacker able to get a trusted CA installed on the device (enterprise MDM, a compromised root, or a user tricked into installing one) can MITM the TLS layer and observe traffic metadata/timing and inject/withhold responses at the transport layer, though not decrypt E2E-encrypted payload content. **Operational dependency:** none from this session; implementing pinning would require establishing a pin-rotation process tied to the app's release cadence, which is a future infrastructure decision, not a source change this session can make. |
| S08-I3 | **Fixed** (S3-11) | Verified | Worker `ACAO: *` while allowing `Authorization` header — `worker/src/index.js:60-62` still sets both | **Fix in `worker/src/index.js` (`corsHeaders()`):** `Access-Control-Allow-Origin` is now only ever set to a specific, explicitly allow-listed origin read from `CORS_ALLOWED_ORIGINS` (comma-separated, documented in `wrangler.jsonc`) that matches the request's `Origin` header — never `*` — removing the "ACAO: `*` + `Authorization` allowed" combination entirely; when no `Origin` is sent or it isn't on the allow-list, `Access-Control-Allow-Origin` is simply omitted (the Android client, which never sends `Origin` and ignores CORS, is unaffected either way). `Vary: Origin` is set whenever ACAO is set, so caches don't leak one origin's response to another. **Evidence:** `grep -n "Access-Control-Allow-Origin" src/index.js` shows the single call site is conditional on `allowList.includes(origin)`, never a literal `'*'`; `npm test` 9/9 pass. See `security-remediation/sessions/SESSION-S3-11.md`. |

---

## Summary

| Sev | Total | Fixed | Partial | Open | Accepted |
|---|---|---|---|---|---|
| Critical | 4 | **4** | 0 | **0** | 0 |
| High | 30 | 19 | 9 | 2 | 0 |
| Medium | 26 | 15 | 10 | 0 | 1 |
| Low | 33 | 18 | 13 | 1 | 1 |
| Info | 23 | 8 | 2 | 3 | 10 |
| **Total** | **116** | **64** | **34** | **6** | **12** |

> **Rollup reconciliation (S3-20 — this pass owns the end-to-end
> reconciliation the previous caveat deferred).** Every row in every section
> above was counted directly from the table structure (`^\s*\|\s*S[A-Z0-9-]+`),
> not carried from any prior tracker or header claim, then cross-checked
> against each row's own Status cell. Two things fell out of that recount that
> are **not** new S3-20 findings — they are pre-existing miscounts in this
> file's own section headers/summary, caught while doing the arithmetic this
> row-count required:
> 1. **`## Medium (26)`'s header count was stale.** The section holds **25**
>    rows, not 26 — there is no 26th Medium row anywhere in the file or in
>    `audit/`; recounting twice (raw grep and a structured parse) both landed
>    on 25. The header text itself is corrected below in the section title.
> 2. **The grand total of 116 inherited that same off-by-one.** Critical(4) +
>    High(30) + Medium(25) + Low(33) + Info(23) = **115**, not 116.
>
> **Post-S3-20 addendum (S06-M6 row added).** Read points 1 and 2 above as a
> historical record of the S3-20 recount, not as the current arithmetic. A
> genuinely new Medium row — `S06-M6`, forced post-unfreeze credential
> rotation — has since been added, taking Medium to **26 rows** and the grand
> total to **116**. Those are the same two numbers S3-20 corrected *away* from,
> which is confusing enough to state explicitly: the current 26/116 is a real
> count of real rows (Medium is 25 pre-existing + S06-M6), **not** a
> reintroduction of the off-by-one, and the old 26/116 was wrong for a
> different reason (a phantom row that never existed). If a future pass
> recounts and lands on 26/116, it agrees with this file; if it lands on
> 25/115, it has missed the `S06-M6` row.
> 3. **Two High-governed IDs live outside the numeric `S0x-H*` pattern:**
>    `S04-M1 (gov: High)` and `S07-M1 (gov: High, = S08-H5)` are filed under
>    the `## High` section precisely because their *governing* severity
>    (per this file's own Legend: "Sev — governing severity") is High despite
>    the `-M1` in their ID — both are counted in the High row above (`S04-M1`
>    → Fixed, `S07-M1` → Open, matching `S08-H5`'s disposition since they are
>    the same underlying defect).
>
> No row's individual Status/Confidence/Evidence text was changed by this
> reconciliation pass beyond the 13 accepted-finding rows this session
> reviewed on their own merits (see the note below the Informational table and
> `DECISION-LOG.md`). This table is now a straight sum of the rows as they
> stand after those 13 edits — recount it directly against the section bodies
> if a future session doubts it, using the same anchor pattern noted above. 

This is meaningfully worse than the old `MASTER_CHECKLIST.md`'s last snapshot
(0% complete, everything reset) and meaningfully better than
`FINDING_INDEX.md`'s row-by-row claims in five specific places (table above).
Neither old document reflected the actual repo at any single point in time —
this file does, as of `298f916`.

## UX findings (`audit/UX_REVIEW.md`)

These come from the UX review, **not** the security audit, and are deliberately
kept out of every count and reconciliation total above — the `## Severity
summary` numbers cover `S0x-*`/`SC-*`/`S10-*` security rows only. Do not fold
these rows into them. `audit/UX_REVIEW.md` holds the full narrative; this table
carries the disposition.

| ID | Status | Confidence | Issue | Evidence |
|---|---|---|---|---|
| UX-1 | **Partial** | Verified | Verification result ephemeral — no persisted "Verified" state | New `app/src/main/java/com/duoshield/app/util/VerificationStore.java` persists `verified_at_<uid>` + `verified_fp_<uid>` in `duoshield_prefs` — the same prefs file as `safety_num_changed_<uid>`, so the "changed" flag and the "verified" record are written/cleared together and cannot drift. `KeyFingerprintActivity.renderVerificationState()` renders a verified pill (`bg_verified_pill` + `ic_verified_shield` + `ds_verified` colors) and is invoked from `onResume()`, which always runs after `onCreate()`, so the badge is correct on first paint as well as on return. Stale-badge-after-key-rotation is closed two independent ways: `DuoShieldSignalStore` calls `VerificationStore.clearVerification()` at **both** identity-key-change sites, and `isStale()` compares the stored fingerprint against the current one. **Partial because** the review also asked for the badge on the contact-detail screen and the chat toolbar; only the fingerprint screen shows it. Durable state itself is done. |
| UX-2 | **Fixed** | Verified | Key-change warning was a dismissable one-shot; ✕ read as equivalent to VERIFY | `safetyNumberBanner` in `activity_chat_media.xml` is a persistent banner driven by `safety_num_changed_<uid>`. ✕ now opens an "Ignore this warning?" confirmation stating that dismissing verifies nothing and that the warning will return, with "Verify now" in the positive slot and "Ignore for now" in the negative — visibly and semantically distinct from VERIFY. The decisive part: neither hide path clears the underlying flag; only a successful QR match in `KeyFingerprintActivity` (via `clear_safety_num_on_match`) does, so the banner genuinely returns on the next `onResume()` until the contact is actually verified. |
| UX-5 | **Fixed** | Verified | Emoji used as icons in trust-critical copy | All trust-critical emoji replaced with real vectors: `🔒` → `ic_lock` in `activity_seed_phrase_display.xml` (decorative, `importantForAccessibility="no"`) and `activity_incoming_call.xml` (icon+label grouped so TalkBack says "End-to-end encrypted" once); `⚠` → `ic_warning` in the `activity_chat_media.xml` safety banner (excluded from a11y tree — adjacent text carries it); `✅`/`❌` dropped from `KeyFingerprintActivity` dialog copy in favour of the UX-1 pill; `🔒` in `ChatMediaActivity.updateOnlineStatus()` and `⚠` in `SeedPhraseDisplayActivity.showMnemonicQrDialog()` are now tinted compound drawables. Both compound-drawable sites `mutate()` before tinting (so the tint cannot leak to other users of the shared cached constant state) and set explicit bounds, because both vectors are 24dp intrinsic — far too large beside 12–13sp text. Non-trust-critical `⚠` left as-is by decision: TURN bandwidth cost warnings, backup-staleness labels, and the exported-ZIP README notice. |
| UX-8 | **Fixed** | Verified | Fingerprint `TextView`s had no `contentDescription` — TalkBack read raw hex per character | Both monospace fingerprint views get a `contentDescription` via `verify_a11y_your_fingerprint` / `verify_a11y_partner_fingerprint`, fed by a `spokenFingerprint()` helper in `KeyFingerprintActivity` that groups the hex for readable speech. Applied at both render points (own + partner) so neither path regresses. |
| UX-banner-tokens | **Fixed** | Verified | Hardcoded hex in the safety banner bypassed the `ds_*` token system | `#CC7B3A00` / `#FFFFD54F` / `#FFFFFFFF` replaced with `ds_warning_banner_bg` / `_text` / `_action` / `_icon` / `_dismiss` in `colors.xml`. The banner's dismiss button `contentDescription` was also wrong — it announced "Warning" rather than the action — and now uses `safety_dismiss_a11y` ("Dismiss safety number warning"). |

**Not closed, deliberately:** UX-6 (explicit "resume setup" affordance), UX-9
(`announceForAccessibility` on step labels), UX-10 (48dp touch-target sweep),
and the two remaining UX-1 badge placements.

**Verification caveat for every UX row above:** source-verified and statically
checked, **not compiled or instrumentation-tested** — same environment
constraint as `S08-H2`/`S08-H3` (no JDK, no Android SDK, no root to install
one). Checks actually run: brace/paren balance on each touched Java file; XML
well-formedness of each touched layout and `strings.xml`; every
`@drawable`/`@color`/`@string` referenced by the touched layouts resolves;
every `R.id.*` used by `KeyFingerprintActivity` and the banner code exists in
its layout; `xmlns:app` declared in all 31 layouts that use `app:tint`; and
`minifyEnabled true` / `shrinkResources true` / `debuggable false` /
`targetSdk 34` / `versionCode 5` intact in `app/build.gradle`.

## Next steps for anyone picking this up

1. Rows marked **Carried** were not re-read against source in this pass —
   re-verify before relying on them, same discipline that caught the five
   corrections above.
2. Highest-value next fixes by exposure: **SC-01** (unverified crypto JAR —
   the last open Critical), **S08-H5/S07-M1** (plaintext key fallback),
   **S01-H1/H2/H3** (Firestore rules gaps with no code dependency, cheap to
   close), **S07-H2/H3** (backup oracle, missing AAD).
3. `SC-04`, `SC-05`, `SC-12` need an operator with GitHub Actions/branch
   settings access — they can't be closed from source alone.
4. **Ship-readiness after the readiness-fix pass.** The self-documented release
   blocker (the `S08-H2` screenshot-default regression from `9196a75`) is
   reverted in source, and the five UX items are closed or reduced to named
   polish. Two things still gate a ship decision, and neither is a code fix:
   - **Run a real build.** Everything in that pass is source-verified only —
     no environment here has a JDK or Android SDK. `assembleRelease` plus an
     on-device check that a fresh install starts with `FLAG_SECURE` set is
     required before `S08-H2` can go to `Fixed`.
   - **`S08-H5` / `S07-M1` (`SecurePrefs` plaintext fallback) is still `Open`
     and needs a product decision, not code** — removing the fallback trades a
     plaintext key copy on disk against a hard lockout on budget/Android-Go
     devices, which needs a user-facing consent flow. It was deliberately left
     untouched by the readiness-fix pass. It is a High row on the identity key,
     backup key, and SQLCipher passphrase, so it should be decided explicitly
     rather than inherited by silence.
